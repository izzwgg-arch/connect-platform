; *********************************************************************************
; @Date : Sun May 3 21:39:03 GMT 2026
; @Document : extensions__50-1-features.conf
; @Author : VitalPBX
; @Platform : VitalPBX
; *********************************************************************************

[feature-account_code]
exten => *78,1,NoOp(Customer Code)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-account-code,s,1)
 same => n,Hangup()

[feature-boss_secretary]
exten => *36,1,NoOp(Boss/Secretary - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-boss-secretary-state,s,1)
 same => n,Hangup()

[feature-attended_transfer]
exten => *2,1,NoOp(Attended Transfer)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(atxfer,s,1)
 same => n,Hangup()

[feature-one_touch_rec]
exten => *3,1,NoOp(One Touch Recording)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(automixmon,s,1)
 same => n,Hangup()

[feature-auth_code]
exten => *79,1,NoOp(Authorization Code)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-autorization-code,s,1)
 same => n,Hangup()

[feature-add_num_blacklist]
exten => *30,1,NoOp(Blacklist a Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-blacklist-add,s,1)
 same => n,Hangup()

[feature-add_last_caller_blacklist]
exten => *32,1,NoOp(Blacklist Last Caller)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-blacklist-last,s,1)
 same => n,Hangup()

[feature-remove_number_blacklist]
exten => *31,1,NoOp(Remove Number From Blacklist)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-blacklist-remove,s,1)
 same => n,Hangup()

[feature-blind_transfer]
exten => #1,1,NoOp(Blind Transfer)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(blindxfer,s,1)
 same => n,Hangup()

[feature-cancel_cc]
exten => *41,1,NoOp(Cancel Call Completion)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-call-completion-cancel,s,1)
 same => n,Hangup()

[feature-request_cc]
exten => *40,1,NoOp(Call Completion - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-call-completion-state,s,1(ombu-call-completion,CC))
 same => n,Hangup()

[feature-set_cfb_number]
exten => *63,1,NoOp(Set CF Busy Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-set,s,1(call-fwd-on-busy,CFB))
 same => n,Hangup()

[feature-toggle_cfb]
exten => *62,1,NoOp(Call Forward Busy - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-state,s,1(call-fwd-on-busy,CFB))
 same => n,Hangup()

exten => _*62*[+*#0-9]!,1,NoOp(Call Forward Busy - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-state-direct,s,1(3,call-fwd-on-busy,CFB))
 same => n,Hangup()

[feature-set_cfu_number]
exten => *61,1,NoOp(Set CF Unavailable Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-set,s,1(call-forward&unavailable,CFU))
 same => n,Hangup()

[feature-toggle_cfu]
exten => *60,1,NoOp(Call Forward Unavailable - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-state,s,1(call-forward&unavailable,CFU))
 same => n,Hangup()

exten => _*60*[+*#0-9]!,1,NoOp(Call Forward Unavailable - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-state-direct,s,1(3,call-forward&unavailable,CFU))
 same => n,Hangup()

[feature-toggle_cfn]
exten => *64,1,NoOp(Call Forward On No Answer - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-state,s,1(call-fwd-no-ans,CFN))
 same => n,Hangup()

exten => _*64*[+*#0-9]!,1,NoOp(Call Forward On No Answer - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-state-direct,s,1(3,call-fwd-no-ans,CFN))
 same => n,Hangup()

[feature-set_cfn_number]
exten => *65,1,NoOp(Set CF On No Answer Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-set,s,1(call-fwd-no-ans,CFN))
 same => n,Hangup()

[feature-spy_random_chn]
exten => *57,1,NoOp(Spy Random Channels)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

[feature-change_ext_pwd]
exten => *76,1,NoOp(Change Features Password)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-change-password,s,1)
 same => n,Hangup()

[feature-clear_all_diversions]
exten => *69,1,NoOp(Clear all Diversions)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cfclear-all,s,1)
 same => n,Hangup()

[feature-cust_recording]
exten => *92,1,NoOp(Custom Recording)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-custom-recording,s,1)
 same => n,Hangup()

[feature-dictation_services]
exten => *93,1,NoOp(Dictation)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-dictate-record,s,1)
 same => n,Hangup()

[feature-direct_vm]
exten => *97,1,NoOp(Direct Voicemail)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-direct-voicemail,s,1)
 same => n,Hangup()

[feature-direct_pickup]
exten => _*07[0-9]!,1,NoOp(Direct Pickup)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-direct-pickup,s,1(3))
 same => n,Hangup()

[feature-disconnect_call]
exten => *0,1,NoOp(Disconnect Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(disconnect,s,1)
 same => n,Hangup()

[feature-dnd]
exten => *66,1,NoOp(Do Not Disturb - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-dnd-state,s,1)
 same => n,Hangup()

exten => _*66*[+*#0-9]!,1,NoOp(Do Not Disturb - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-dnd-state-direct,s,1(3))
 same => n,Hangup()

[feature-echo_test]
exten => *72,1,NoOp(Echo Test)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-echotest,s,1)
 same => n,Hangup()

[feature-follow_me]
exten => *67,1,NoOp(Follow Me - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-followme-state,s,1)
 same => n,Hangup()

[feature-hot_desking]
exten => *80,1,NoOp(Hot Desking)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-hot-desking,s,1)
 same => n,Hangup()

exten => _*80*[+*#0-9]!,1,NoOp(Hot Desking - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-hot-desking-direct,s,1(3))
 same => n,Hangup()

[feature-dial_by_name_dir]
exten => 411,1,NoOp(Dial By Name Directory)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-internal-directory,s,1)
 same => n,Hangup()

[feature-lock_unlock_phone]
exten => *75,1,NoOp(Lock/Unlock Phone)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-lock-phone-state,s,1)
 same => n,Hangup()

[feature-nm_all]
exten => *81,1,NoOp(Night Mode All)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-night-mode-state,s,1(nm_all))
 same => n,Hangup()

[feature-park_call]
exten => *4,1,NoOp(Park Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(parkcall,s,1)
 same => n,Hangup()

[feature-personal_assistant]
exten => *96,1,NoOp(Personal Assistant - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-personal-assistant-state,s,1)
 same => n,Hangup()

[feature-pickup_group]
exten => *08,1,NoOp(Pickup Group)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-pickup-group,s,1)
 same => n,Hangup()

[feature-rec_msg_pa]
exten => *94,1,NoOp(Record Msg For Personal Assistant)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-personal-assistant-rec-msg,s,1)
 same => n,Hangup()

[feature-reminder]
exten => *38,1,NoOp(Reminder)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-reminder,s,1)
 same => n,Hangup()

[feature-remote_substitution]
exten => *77,1,NoOp(Remote Substitution)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-remote-substitution,s,1)
 same => n,Hangup()

[feature-remote_vm]
exten => *98,1,NoOp(Remote Voicemail)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-remote-voicemail,s,1)
 same => n,Hangup()

exten => _*98*[+*#0-9]!,1,NoOp(Remote Voicemail - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-remote-voicemail-direct,s,1(3))
 same => n,Hangup()

[feature-remote_wakeup_call]
exten => *35,1,NoOp(Remote Wakeup Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

[feature-say_date_time]
exten => *70,1,NoOp(Speak Date and Time)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-say-clock,s,1)
 same => n,Hangup()

[feature-simulate_incoming_call]
exten => *73,1,NoOp(Simulate Incoming Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-simulate-incoming-call,s,1)
 same => n,Hangup()

[feature-speak_last_number]
exten => *37,1,NoOp(Speak Last Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-speak-last-number,s,1)
 same => n,Hangup()

[feature-speak_ext_number]
exten => *71,1,NoOp(Speak Your Extension Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-speakextennum,s,1)
 same => n,Hangup()

[feature-spy_extension]
exten => *55,1,NoOp(Spy on Extension)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

[feature-spy_ext_whisper]
exten => *56,1,NoOp(Spy on Extension In Whisper Mode)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

[feature-add_remove_queue_agent]
exten => *50,1,NoOp(Add/Remove Queue Agent)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-queue-agent,s,1)
 same => n,Hangup()

exten => _*50*[+*#0-9]!,1,NoOp(Add/Remove Queue Agent - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-queue-agent-direct,s,1(3))
 same => n,Hangup()

[feature-pause_unpause_queue_agent]
exten => *51,1,NoOp(Pause/Unpause Queue Agent)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-queue-pause-agent,s,1)
 same => n,Hangup()

exten => _*51*[+*#0-9]!,1,NoOp(Pause/Unpause Queue Agent - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-queue-pause-agent-direct,s,1(3))
 same => n,Hangup()

[feature-request_wakeup_call]
exten => *34,1,NoOp(Wakeup Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-wake-up-call-request,s,1)
 same => n,Hangup()

[feature-toggle_cfi]
exten => *58,1,NoOp(Call Forward Immediately - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-state,s,1(call-fwd-unconditional,CFI))
 same => n,Hangup()

exten => _*58*[+*#0-9]!,1,NoOp(Call Forward Immediately - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-state-direct,s,1(3,call-fwd-unconditional,CFI))
 same => n,Hangup()

[feature-set_cfi_number]
exten => *59,1,NoOp(Set CF Immediately Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-set,s,1(call-fwd-unconditional,CFI))
 same => n,Hangup()

exten => _*59*[+*#0-9]!,1,NoOp(Set CF Immediately Number - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-cf-set-direct,s,1(3,call-fwd-unconditional,CFI))
 same => n,Hangup()

[feature-queues_login_logout]
exten => *52,1,NoOp(Queues Login/Logout)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-queues-login-logout,s,1)
 same => n,Hangup()

exten => _*52*[+*#0-9]!,1,NoOp(Queues Login/Logout - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-queues-login-logout-direct,s,1(3))
 same => n,Hangup()

[feature-queues-pause-unpause]
exten => *53,1,NoOp(Queues Pause/Unpause)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-queues-pause-unpause,s,1)
 same => n,Hangup()

exten => _*53*[+*#0-9]!,1,NoOp(Queues Pause/Unpause - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-queues-pause-unpause-direct,s,1(3))
 same => n,Hangup()

[feature-send_vm_msg]
exten => _*95*[+*#0-9].,1,NoOp(Send Voicemail Message)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(send-vm-msg,s,1(3))
 same => n,Hangup()

[feature-spy_ext_barge]
exten => *54,1,NoOp(Spy on Extension In Barge Mode)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

[feature-paging_and_intercom]
exten => _*82[+#0-9].,1,NoOp(Paging)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-paging-and-intercom,s,1(3))
 same => n,Hangup()

[feature-hot_desking_cc]
exten => *90,1,NoOp(Hot Desking CC)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-hot-desking-cc,s,1)
 same => n,Hangup()

exten => _*90#[+*0-9]!,1,NoOp(Hot Desking CC - Extension)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-hot-desking-cc,s,1(3,extension))
 same => n,Hangup()

exten => _*90#[+*0-9]!#[*0-9].,1,NoOp(Hot Desking CC - Extension & Password)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-hot-desking-cc,s,1(3,extension_pwd))
 same => n,Hangup()

[feature-paging_duplex]
exten => _*83[+#0-9].,1,NoOp(Paging Duplex)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-paging-duplex,s,1(3))
 same => n,Hangup()

[feature-anonymous_calling]
exten => _*88[+#0-9].,1,NoOp(Anonymous Calling)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(sub-anonymous-calling,s,1(3))
 same => n,Hangup()

[feature-auto_recording_switch_in]
exten => *1,1,NoOp(Pause/Resume Incoming Call Recordings)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(auto_recording_switch,s,1)
 same => n,Hangup()

[feature-auto_recording_switch_out]
exten => *00,1,NoOp(Pause/Resume Outbound Call Recordings)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Gosub(auto_recording_switch,s,1)
 same => n,Hangup()

[not-allowed-features]
exten => *78,1,NoOp(Customer Code)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *36,1,NoOp(Boss/Secretary - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *2,1,NoOp(Attended Transfer)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *3,1,NoOp(One Touch Recording)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *79,1,NoOp(Authorization Code)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *30,1,NoOp(Blacklist a Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *32,1,NoOp(Blacklist Last Caller)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *31,1,NoOp(Remove Number From Blacklist)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => #1,1,NoOp(Blind Transfer)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *41,1,NoOp(Cancel Call Completion)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *40,1,NoOp(Call Completion - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *63,1,NoOp(Set CF Busy Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *62,1,NoOp(Call Forward Busy - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*62*[+*#0-9]!,1,NoOp(Call Forward Busy - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *61,1,NoOp(Set CF Unavailable Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *60,1,NoOp(Call Forward Unavailable - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*60*[+*#0-9]!,1,NoOp(Call Forward Unavailable - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *64,1,NoOp(Call Forward On No Answer - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*64*[+*#0-9]!,1,NoOp(Call Forward On No Answer - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *65,1,NoOp(Set CF On No Answer Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *57,1,NoOp(Spy Random Channels)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *76,1,NoOp(Change Features Password)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *69,1,NoOp(Clear all Diversions)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *92,1,NoOp(Custom Recording)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *93,1,NoOp(Dictation)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *97,1,NoOp(Direct Voicemail)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*07[0-9]!,1,NoOp(Direct Pickup)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *0,1,NoOp(Disconnect Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *66,1,NoOp(Do Not Disturb - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*66*[+*#0-9]!,1,NoOp(Do Not Disturb - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *72,1,NoOp(Echo Test)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *67,1,NoOp(Follow Me - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *80,1,NoOp(Hot Desking)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*80*[+*#0-9]!,1,NoOp(Hot Desking - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => 411,1,NoOp(Dial By Name Directory)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *75,1,NoOp(Lock/Unlock Phone)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *81,1,NoOp(Night Mode All)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *4,1,NoOp(Park Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *96,1,NoOp(Personal Assistant - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *08,1,NoOp(Pickup Group)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *94,1,NoOp(Record Msg For Personal Assistant)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *38,1,NoOp(Reminder)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *77,1,NoOp(Remote Substitution)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *98,1,NoOp(Remote Voicemail)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*98*[+*#0-9]!,1,NoOp(Remote Voicemail - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *35,1,NoOp(Remote Wakeup Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *70,1,NoOp(Speak Date and Time)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *73,1,NoOp(Simulate Incoming Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *37,1,NoOp(Speak Last Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *71,1,NoOp(Speak Your Extension Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *55,1,NoOp(Spy on Extension)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *56,1,NoOp(Spy on Extension In Whisper Mode)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *50,1,NoOp(Add/Remove Queue Agent)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*50*[+*#0-9]!,1,NoOp(Add/Remove Queue Agent - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *51,1,NoOp(Pause/Unpause Queue Agent)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*51*[+*#0-9]!,1,NoOp(Pause/Unpause Queue Agent - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *34,1,NoOp(Wakeup Call)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *58,1,NoOp(Call Forward Immediately - Toggle)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*58*[+*#0-9]!,1,NoOp(Call Forward Immediately - Toggle - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *59,1,NoOp(Set CF Immediately Number)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*59*[+*#0-9]!,1,NoOp(Set CF Immediately Number - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *52,1,NoOp(Queues Login/Logout)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*52*[+*#0-9]!,1,NoOp(Queues Login/Logout - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *53,1,NoOp(Queues Pause/Unpause)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*53*[+*#0-9]!,1,NoOp(Queues Pause/Unpause - Direct)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*95*[+*#0-9].,1,NoOp(Send Voicemail Message)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *54,1,NoOp(Spy on Extension In Barge Mode)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*82[+#0-9].,1,NoOp(Paging)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *90,1,NoOp(Hot Desking CC)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*90#[+*0-9]!,1,NoOp(Hot Desking CC - Extension)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*90#[+*0-9]!#[*0-9].,1,NoOp(Hot Desking CC - Extension & Password)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*83[+#0-9].,1,NoOp(Paging Duplex)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => _*88[+#0-9].,1,NoOp(Anonymous Calling)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *1,1,NoOp(Pause/Resume Incoming Call Recordings)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

exten => *00,1,NoOp(Pause/Resume Outbound Call Recordings)
 same => n,Set(CHANNEL(hangup_handler_push)=local-call-hangup,s,1)
 same => n,Playback(vpbx/vital-feature-disabled)
 same => n,Hangup()

