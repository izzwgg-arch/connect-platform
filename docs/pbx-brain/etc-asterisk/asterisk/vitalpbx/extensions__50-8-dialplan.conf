; *********************************************************************************
; @Date : Wed Jun 3 14:26:18 GMT 2026
; @Document : extensions__50-8-dialplan.conf
; @Author : VitalPBX
; @Platform : VitalPBX
; *********************************************************************************

[T8_ext-followme]
exten => FW102,1,NoOp(Follow Me: FW102)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/102/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW102:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW103,1,NoOp(Follow Me: FW103)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/103/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW103:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW105,1,NoOp(Follow Me: FW105)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/105/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW105:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW106,1,NoOp(Follow Me: FW106)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/106/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW106:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW107,1,NoOp(Follow Me: FW107)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/107/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW107:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW101,1,NoOp(Follow Me: FW101)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/101/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW101:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW104,1,NoOp(Follow Me: FW104)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/104/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW104:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW108,1,NoOp(Follow Me: FW108)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/108/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW108:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW111,1,NoOp(Follow Me: FW111)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/111/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW111:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW112,1,NoOp(Follow Me: FW112)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/112/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW112:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW114,1,NoOp(Follow Me: FW114)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/114/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW114:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW897,1,NoOp(Follow Me: FW897)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/897/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW897:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW109,1,NoOp(Follow Me: FW109)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/109/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW109:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => FW898,1,NoOp(Follow Me: FW898)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/898/language)}))
 same => n,Set(__RETURN_ON_EXTERNAL=yes)
 same => n,Set(__SKIP_PLAYBACK=${IF($["${QUEUE_CALL}"="TRUE"]?TRUE:${SKIP_PLAYBACK})})
 same => n,Set(CALLER_RECORDING=${ASTSPOOLDIR}/tmp/followme-${UNIQUEID}.wav)
 same => n,Set(__O_RING_TIME=30)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__SRC_APP=${IF($["${LEN(${SRC_APP})}"="0"]?FW898:${SRC_APP})})
 same => n(start-dialing),NoOp(Start Dialing)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Return()

exten => h,1,NoOp(Finish Follow-me call)
 same => n,System(rm -f ${CALLER_RECORDING})

[T8_FW102-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/102/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW103-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/103/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW105-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/105/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW106-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/106/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW107-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/107/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW101-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/101/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW104-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/104/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW108-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/108/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW111-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/111/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW112-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/112/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW114-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/114/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW897-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/897/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW109-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/109/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_FW898-confirm]
exten => s,1,NoOp(Confirm Call)
 same => n,ExecIf($["${LEN(${INBOUND_LANGUAGE})}"!="0"]?Set(CHANNEL(language)=${INBOUND_LANGUAGE}):Set(CHANNEL(language)=${DB(${TENANT}/extensions/898/language)}))
 same => n,Set(DIALED_NUMBER=${CUT(DIALEDPEERNUMBER,@,1)})
 same => n,Set(SOUND=${IF($["${LEN(${FWM_RECORDED_NAME})}"="0"]?followme/no-recording:followme/call-from&${FWM_RECORDED_NAME})})

[T8_extvm-operator]
exten => VMO102,1,NoOp(Voicemail Operator for extension 102)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO103,1,NoOp(Voicemail Operator for extension 103)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO105,1,NoOp(Voicemail Operator for extension 105)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO106,1,NoOp(Voicemail Operator for extension 106)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO107,1,NoOp(Voicemail Operator for extension 107)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO101,1,NoOp(Voicemail Operator for extension 101)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO104,1,NoOp(Voicemail Operator for extension 104)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO108,1,NoOp(Voicemail Operator for extension 108)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO111,1,NoOp(Voicemail Operator for extension 111)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO112,1,NoOp(Voicemail Operator for extension 112)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO114,1,NoOp(Voicemail Operator for extension 114)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO897,1,NoOp(Voicemail Operator for extension 897)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO109,1,NoOp(Voicemail Operator for extension 109)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

exten => VMO898,1,NoOp(Voicemail Operator for extension 898)
 same => n,Playback(disabled)
 same => n,Return()
 same => n,Hangup()

[T8_app-custom-application]
exten => 303,1,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,NoOp(Custom Application: Recruiting)
 same => n,Goto(sub-extensions-vm,VM-107,1)
 same => n,Hangup()

exten => 96669,1,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,NoOp(Custom Application: Disa)
 same => n,Goto(T8_app-disa,DISA-15,1)
 same => n,Hangup()

[T8_ext-parking]
include => parking-8-parkedcalls

exten => 700,1,NoOp(Parking Call)
 same => n,Park(parking-8,c(parking-8-callback,s,1))

exten => _70[1-9],1,NoOp(Slot: ${CALL_DESTINATION})
 same => n,Set(RECORD_PARKING_LOT=no)
 same => n,Gosub(sub-parking-lots,s,1(${CALL_DESTINATION},parking-8,parking-8-parkedcalls))

exten => _71[0-9],1,NoOp(Slot: ${CALL_DESTINATION})
 same => n,Set(RECORD_PARKING_LOT=no)
 same => n,Gosub(sub-parking-lots,s,1(${CALL_DESTINATION},parking-8,parking-8-parkedcalls))

exten => _720,1,NoOp(Slot: ${CALL_DESTINATION})
 same => n,Set(RECORD_PARKING_LOT=no)
 same => n,Gosub(sub-parking-lots,s,1(${CALL_DESTINATION},parking-8,parking-8-parkedcalls))

[parking-8-parkedcallstimeout]
exten => s,1,NoOp(Parking Timeout has been reached)
 same => n,Gosub(T8_cos-all,102,1)
 same => n,Hangup()

[parking-8-callback]
exten => s,1,NoOp(Returning Call)
 same => n,Set(CALLBACK_EXT=${IF($["${CALL_TYPE}"="3"]?${CALLER}:${DESTINATION_NUMBER})})
 same => n,Set(CALLBACK_CTXT=${TRANSFER_CONTEXT})
 same => n,GotoIf($[$["${LEN(${CALLBACK_EXT})}"="0"]|$["${LEN(${CALLBACK_CTXT})}"="0"]]?end)
 same => n,Goto(${CALLBACK_CTXT},${CALLBACK_EXT},1)
 same => n(end),Hangup()

[T8_applications]
;include => T8_app-speedial
include => T8_app-custom-application
;include => T8_app-custom-destination
;include => T8_app-paging
;include => T8_app-vmgroup
;include => T8_app-queues-priority
include => T8_app-disa
include => T8_app-ivr
include => T8_app-announcement
;include => T8_app-languages
;include => T8_app-nightmode
;include => T8_app-call-back
;include => T8_app-rec-management
include => T8_app-time-condition
include => T8_app-ai-assistants


[T8_extensions]
;include => T8_ext-conferences
include => T8_ext-parking
include => T8_ext-ringgroups
include => T8_ext-queues


[T8_hot-desking-context]
exten => *80,1,Set(CDR(source)=${CALLERID(num)})
 same => n,Set(CDR(destination)=${EXTEN})
 same => n,Set(CDR(tenant)=gesheft)
 same => n,NoOp(Hot Desking Feature)
 same => n,Set(__TENANT=106048d48cb4ddf6)
 same => n,Set(__TENANT_PREFIX=T8_)
 same => n,Gosub(T8_set-global-tenant-vars,s,1)
 same => n,Gosub(sub-hot-desking,s,1)
 same => n,Hangup()

exten => _*80*[0-9]!,1,Gosub(sub-get-device-tree,s,1)
 same => n,Set(__CALL_DESTINATION=${EXTEN})
 same => n,Set(CDR(source)=${CALLERID(num)})
 same => n,Set(CDR(destination)=${EXTEN})
 same => n,Set(CDR(tenant)=gesheft)
 same => n,NoOp(Hot Desking Direct Feature)
 same => n,Set(__TENANT=106048d48cb4ddf6)
 same => n,Set(__TENANT_PREFIX=T8_)
 same => n,Gosub(T8_set-global-tenant-vars,s,1)
 same => n,Gosub(feature-hot_desking,${EXTEN},1)
 same => n,Hangup()

exten => *90,1,Gosub(sub-get-device-tree,s,1)
 same => n,Set(__CALL_DESTINATION=${EXTEN})
 same => n,Set(CDR(source)=${CALLERID(num)})
 same => n,Set(CDR(destination)=${EXTEN})
 same => n,Set(CDR(tenant)=gesheft)
 same => n,NoOp(Hot Desking CC Feature)
 same => n,Set(__TENANT=106048d48cb4ddf6)
 same => n,Set(__TENANT_PREFIX=T8_)
 same => n,Gosub(T8_set-global-tenant-vars,s,1)
 same => n,Gosub(feature-hot_desking_cc,${EXTEN},1)
 same => n,Hangup()

exten => _*90#[+*0-9]!,1,Gosub(sub-get-device-tree,s,1)
 same => n,Set(__CALL_DESTINATION=${EXTEN})
 same => n,Set(CDR(source)=${CALLERID(num)})
 same => n,Set(CDR(destination)=${EXTEN})
 same => n,Set(CDR(tenant)=gesheft)
 same => n,NoOp(Hot Desking CC Feature)
 same => n,Set(__TENANT=106048d48cb4ddf6)
 same => n,Set(__TENANT_PREFIX=T8_)
 same => n,Gosub(T8_set-global-tenant-vars,s,1)
 same => n,Gosub(feature-hot_desking_cc,${EXTEN},1)
 same => n,Hangup()

exten => _*90#[+*0-9]!#[*0-9].,1,Gosub(sub-get-device-tree,s,1)
 same => n,Set(__CALL_DESTINATION=${EXTEN})
 same => n,Set(CDR(source)=${CALLERID(num)})
 same => n,Set(CDR(destination)=${EXTEN})
 same => n,Set(CDR(tenant)=gesheft)
 same => n,NoOp(Hot Desking CC Feature)
 same => n,Set(__TENANT=106048d48cb4ddf6)
 same => n,Set(__TENANT_PREFIX=T8_)
 same => n,Gosub(T8_set-global-tenant-vars,s,1)
 same => n,Gosub(feature-hot_desking_cc,${EXTEN},1)
 same => n,Hangup()

exten => _[-+*#0-9a-zA-Z].,1,Set(CDR(source)=${CALLERID(num)})
 same => n,Set(CDR(destination)=${EXTEN})
 same => n,Set(CDR(tenant)=gesheft)
 same => n,NoOp(Hot Desking)
 same => n,Set(__TENANT=106048d48cb4ddf6)
 same => n,Set(__TENANT_PREFIX=T8_)
 same => n,Gosub(sub-hot-desking-call,s,1(${EXTEN}))

[T8_cos-all]
include => T8_cos-all-init


[T8_cos-all-init]
exten => _[-+*#0-9a-zA-Z].,1,NoOp(More than on digit pattern)
 same => n,Gosub(s,1(${EXTEN}))

exten => _[-+*#0-9a-zA-Z],1,NoOp(One Digit pattern)
 same => n,Gosub(s,1(${EXTEN}))

exten => i,1,NoOp(Invalid dial on init section)
 same => n,ForkCDR(e)
 same => n,ExecIf($[$["${FROM_QUEUE_CALLBACK}"="yes"]|$["${SRC_APP}"="IVR"]]?Hangup():)
 same => n,Goto(invalid-dest-cos,s,1)

exten => h,1,NoCDR()
 same => n,NoOp(Hanging Up the Call)
 same => n,Hangup()

exten => s,1,Set(EXTENSION=${ARG1})
 same => n,NoOp(Dialing ${EXTENSION} from ${CALLERID(num)})
 same => n,Gosub(sub-set-global-vars,s,1(106048d48cb4ddf6,${EXTENSION},parking-8))
 same => n,Gosub(sub-set-call-vars,s,1(106048d48cb4ddf6,${EXTENSION},8,T8_cos-all,T8_ARS-all))
 same => n,Gosub(sub-construct-cid,s,1)
 same => n,Gosub(T8_set-global-tenant-vars,s,1)
 same => n,GotoIf($["${CALL_ORIGIN}"="RESTRICTED_IVR_CALL"]?local-dialing)
 same => n,NoOp(Check if is an Emergency Call)
 same => n,GotoIf($[${DIALPLAN_EXISTS(T8_emergency-calls,${EXTENSION},1)}=1]?T8_emergency-calls,${EXTENSION},1)
 same => n,Gosub(sub-lockphone-check,s,1)
 same => n(local-dialing),Gosub(sub-local-dialing,s,1)
 same => n,GotoIf($["${CALL_ORIGIN}"="RESTRICTED_IVR_CALL"]?end-call)
 same => n,Set(OUTBOUND_PROFILE=${DB(${TENANT}/extensions/${CALL_SOURCE}/outbound_profile)})
 same => n,GotoIf($[$["${OUTBOUND_PROFILE}"="disabled"]|$["X${OUTBOUND_PROFILE}X"="XX"]]?post-dialing)
 same => n,GotoIf($[${DIALPLAN_EXISTS(${OUTBOUND_PROFILE},${EXTENSION},1)}=1]?${OUTBOUND_PROFILE},${EXTENSION},1)
 same => n(post-dialing),Goto(T8_cos-all-post,${EXTENSION},1)
 same => n(end-call),Hangup()

[T8_cos-all-custom]
exten => fake-ext,1,NoOp(Fake extension for generate this context from VitalPBX)

[T8_cos-all-post]
include => T8_all-features-category
include => T8_extensions
include => T8_applications
include => T8_cos-all-custom
include => T8_ARS-all
include => not-allowed-features
include => app-termination

exten => i,1,NoOp(Invalid dial on post section)
 same => n,ForkCDR(e)
 same => n,ExecIf($[$["${FROM_QUEUE_CALLBACK}"="yes"]|$["${SRC_APP}"="IVR"]]?Hangup():)
 same => n,Goto(invalid-dest-cos,s,1)

exten => h,1,NoOp(Hanging Up the Call (Post))
 same => n,Hangup()

[T8_cos-all-trunk]
exten => _[-+*#0-9a-zA-Z].,1,NoOp(Class of Services Trunk: All Permissions)
 same => n,Gosub(sub-check-blacklist,s,1(106048d48cb4ddf6,${CALLERID(num)}))
 same => n,Gosub(sub-setup-call-type,s,1(incoming))
 same => n,Gosub(sub-set-call-vars,s-incoming,1(${CALLERID(num)},${EXTEN},106048d48cb4ddf6))
 same => n,Goto(T8_cos-all,${EXTEN},1)
 same => n,Hangup()

[T8_cos-103]
;include => DRR-1-INTERNAL
include => T8_cos-103-init


[T8_cos-103-init]
exten => _[-+*#0-9a-zA-Z].,1,NoOp(More than on digit pattern)
 same => n,Gosub(s,1(${EXTEN}))

exten => _[-+*#0-9a-zA-Z],1,NoOp(One Digit pattern)
 same => n,Gosub(s,1(${EXTEN}))

exten => i,1,NoOp(Invalid dial on init section)
 same => n,ForkCDR(e)
 same => n,ExecIf($[$["${FROM_QUEUE_CALLBACK}"="yes"]|$["${SRC_APP}"="IVR"]]?Hangup():)
 same => n,Goto(invalid-dest-cos,s,1)

exten => h,1,NoCDR()
 same => n,NoOp(Hanging Up the Call)
 same => n,Hangup()

exten => s,1,Set(EXTENSION=${ARG1})
 same => n,NoOp(Dialing ${EXTENSION} from ${CALLERID(num)})
 same => n,Gosub(sub-set-global-vars,s,1(106048d48cb4ddf6,${EXTENSION},parking-8))
 same => n,Gosub(sub-set-call-vars,s,1(106048d48cb4ddf6,${EXTENSION},14,T8_cos-103,ARS-29))
 same => n,Gosub(sub-construct-cid,s,1)
 same => n,Gosub(T8_set-global-tenant-vars,s,1)
 same => n,GotoIf($["${CALL_ORIGIN}"="RESTRICTED_IVR_CALL"]?local-dialing)
 same => n,NoOp(Check if is an Emergency Call)
 same => n,GotoIf($[${DIALPLAN_EXISTS(T8_emergency-calls,${EXTENSION},1)}=1]?T8_emergency-calls,${EXTENSION},1)
 same => n,Gosub(sub-lockphone-check,s,1)
 same => n(local-dialing),Gosub(sub-local-dialing,s,1)
 same => n,GotoIf($["${CALL_ORIGIN}"="RESTRICTED_IVR_CALL"]?end-call)
 same => n,Set(OUTBOUND_PROFILE=${DB(${TENANT}/extensions/${CALL_SOURCE}/outbound_profile)})
 same => n,GotoIf($[$["${OUTBOUND_PROFILE}"="disabled"]|$["X${OUTBOUND_PROFILE}X"="XX"]]?post-dialing)
 same => n,GotoIf($[${DIALPLAN_EXISTS(${OUTBOUND_PROFILE},${EXTENSION},1)}=1]?${OUTBOUND_PROFILE},${EXTENSION},1)
 same => n(post-dialing),Goto(T8_cos-103-post,${EXTENSION},1)
 same => n(end-call),Hangup()

[T8_cos-103-custom]
exten => fake-ext,1,NoOp(Fake extension for generate this context from VitalPBX)

[T8_cos-103-post]
include => T8_all-features-category
include => T8_extensions
include => T8_applications
include => T8_cos-103-custom
include => DRR-1
include => ARS-29
include => not-allowed-features
include => app-termination

exten => i,1,NoOp(Invalid dial on post section)
 same => n,ForkCDR(e)
 same => n,ExecIf($[$["${FROM_QUEUE_CALLBACK}"="yes"]|$["${SRC_APP}"="IVR"]]?Hangup():)
 same => n,Goto(invalid-dest-cos,s,1)

exten => h,1,NoOp(Hanging Up the Call (Post))
 same => n,Hangup()

[T8_cos-103-trunk]
exten => _[-+*#0-9a-zA-Z].,1,NoOp(Class of Services Trunk: 103)
 same => n,Gosub(sub-check-blacklist,s,1(106048d48cb4ddf6,${CALLERID(num)}))
 same => n,Gosub(sub-setup-call-type,s,1(incoming))
 same => n,Gosub(sub-set-call-vars,s-incoming,1(${CALLERID(num)},${EXTEN},106048d48cb4ddf6))
 same => n,Goto(T8_cos-103,${EXTEN},1)
 same => n,Hangup()

[T8_set-global-tenant-vars]
exten => s,1,NoOp(Setting Global Vars for gesheft Tenant)
 same => n,Set(__TENANT_PATH=106048d48cb4ddf6)
 same => n,Set(__TENANT_PREFIX=T8_)
 same => n,Set(__QUEUE_AGENTS_CONTEXT=T8_queue-call-to-agents)
 same => n,Set(__FOLLOWME_CONTEXT=T8_ext-followme)
 same => n,Set(__HINTS_CONTEXT=T8_extension-hints)
 same => n,Set(__DEFAULT_COS=T8_cos-all)
 same => n,Return()

[T8_all-features-category]
include => feature-account_code
include => feature-boss_secretary
include => feature-attended_transfer
include => feature-one_touch_rec
include => feature-auth_code
include => feature-add_num_blacklist
include => feature-add_last_caller_blacklist
include => feature-remove_number_blacklist
include => feature-blind_transfer
include => feature-cancel_cc
include => feature-request_cc
include => feature-set_cfb_number
include => feature-toggle_cfb
include => feature-set_cfu_number
include => feature-toggle_cfu
include => feature-toggle_cfn
include => feature-set_cfn_number
;include => feature-spy_random_chn
include => feature-change_ext_pwd
include => feature-clear_all_diversions
include => feature-cust_recording
include => feature-dictation_services
include => feature-direct_vm
include => feature-direct_pickup
include => feature-disconnect_call
include => feature-dnd
include => feature-echo_test
include => feature-follow_me
include => feature-hot_desking
include => feature-dial_by_name_dir
include => feature-lock_unlock_phone
include => feature-nm_all
include => feature-park_call
include => feature-personal_assistant
include => feature-pickup_group
include => feature-rec_msg_pa
include => feature-reminder
include => feature-remote_substitution
include => feature-remote_vm
;include => feature-remote_wakeup_call
include => feature-say_date_time
include => feature-simulate_incoming_call
include => feature-speak_last_number
include => feature-speak_ext_number
;include => feature-spy_extension
;include => feature-spy_ext_whisper
include => feature-add_remove_queue_agent
include => feature-pause_unpause_queue_agent
include => feature-request_wakeup_call
include => feature-toggle_cfi
include => feature-set_cfi_number
include => feature-queues_login_logout
include => feature-queues-pause-unpause
include => feature-send_vm_msg
;include => feature-spy_ext_barge
include => feature-paging_and_intercom
include => feature-hot_desking_cc
include => feature-paging_duplex
include => feature-anonymous_calling
include => feature-auto_recording_switch_in
include => feature-auto_recording_switch_out


[DRR-1]
exten => _8453608023,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _18453608023,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _12124441100,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _2124441100,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _8457381100,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _18457381100,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _8454148018,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _18454148018,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _3518887017,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _13518887017,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _8453510404,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

exten => _18453510404,1,Playback(the-number-u-dialed&is&privacy-blocked)
 same => n,Hangup()
 same => n,Goto(${ARS},${EXTEN},1)

[T8_ring-group-dial]
exten => _[-+*#0-9].,1,NoOp(More than on digit pattern)
 same => n,Gosub(s,1(${EXTEN}))

exten => _[-+*#0-9],1,NoOp(One Digit pattern)
 same => n,Gosub(s,1(${EXTEN}))

exten => s,1,NoOp(Dialing Ring Group Member: ${ARG1})
 same => n,Set(EXTENSION=${ARG1})
 same => n,Set(TENANT=106048d48cb4ddf6)
 same => n,Set(COS=${DB(${TENANT}/extensions/${EXTENSION}/context)})
 same => n,GotoIf($["X${COS}X"="XX"]?no-cos)
 same => n,Set(__DISABLE_CF_AA=TRUE)
 same => n,Set(__SKIP_CONTACT_SERVICES=TRUE)
 same => n,Set(__NO_POST_SERVICES=TRUE)
 same => n,Set(__SKIP_PLAYBACK=TRUE)
 same => n,Set(__CALL_ORIGIN=ring-group)
 same => n,Gosub(${COS},${EXTENSION},1)
 same => n,Goto(end-r-dial)
 same => n(no-cos),NoOp(No COS defined for tenant gesheft! Avoiding infinite loop!)
 same => n(end-r-dial),Hangup()

[T8_ext-ringgroups]
exten => 800,1,NoOp(Ring Group: Phone Orders)
 same => n,Set(_IGNORE_DIVERSIONS=no)
 same => n,Set(_SKIP_PLAYBACK=TRUE)
 same => n,Set(__SKIP_AA=TRUE)
 same => n,Set(__NO_POST_SERVICES=TRUE)
 same => n,Set(__SRC_APP=RG800)
 same => n,Set(__PBX_APP=RING_GROUP)
 same => n,Set(__PBX_APP_DESC=Phone Orders)
 same => n,Set(__RG_RINGTIME=30)
 same => n,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,Set(CALLERID(name)=Phone Orders:${CALLERID(name)})
 same => n,Answer()
 same => n,NoCDR()
 same => n,Dial(Local/101@T8_ring-group-dial/n&Local/102@T8_ring-group-dial/n&Local/111@T8_ring-group-dial/n,30,rctTU(clean-variables))
 same => n,ResetCDR(ve)
 same => n,Set(__CALL_ORIGIN=normal)
 same => n,Set(__IGNORE_DIVERSIONS=no)
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Set(__NO_POST_SERVICES=FALSE)
 same => n,Set(__DISABLE_CF_AA=FALSE)
 same => n,Set(__SKIP_AA=FALSE)
 same => n,Set(__SKIP_BUSY=FALSE)
 same => n,Set(__RG_RINGTIME=)
 same => n,Goto(sub-extensions-vm,VM-101,1)

exten => 801,1,NoOp(Ring Group: Missing Boxes)
 same => n,Set(_IGNORE_DIVERSIONS=no)
 same => n,Set(_SKIP_PLAYBACK=TRUE)
 same => n,Set(__SKIP_AA=TRUE)
 same => n,Set(__NO_POST_SERVICES=TRUE)
 same => n,Set(__SRC_APP=RG801)
 same => n,Set(__PBX_APP=RING_GROUP)
 same => n,Set(__PBX_APP_DESC=Missing Boxes)
 same => n,Set(__RG_RINGTIME=30)
 same => n,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,Set(CALLERID(name)=Missing Boxes:${CALLERID(name)})
 same => n,Answer()
 same => n,NoCDR()
 same => n,Dial(Local/109@T8_ring-group-dial/n,30,rcm(moh6)tTU(clean-variables))
 same => n,ResetCDR(ve)
 same => n,Set(__CALL_ORIGIN=normal)
 same => n,Set(__IGNORE_DIVERSIONS=no)
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Set(__NO_POST_SERVICES=FALSE)
 same => n,Set(__DISABLE_CF_AA=FALSE)
 same => n,Set(__SKIP_AA=FALSE)
 same => n,Set(__SKIP_BUSY=FALSE)
 same => n,Set(__RG_RINGTIME=)
 same => n,Goto(sub-extensions-vm,VM-101,1)

exten => 802,1,NoOp(Ring Group: Customer Service)
 same => n,Set(_IGNORE_DIVERSIONS=no)
 same => n,Set(_SKIP_PLAYBACK=TRUE)
 same => n,Set(__SKIP_AA=TRUE)
 same => n,Set(__NO_POST_SERVICES=TRUE)
 same => n,Set(__SRC_APP=RG802)
 same => n,Set(__PBX_APP=RING_GROUP)
 same => n,Set(__PBX_APP_DESC=Customer Service)
 same => n,Set(__RG_RINGTIME=30)
 same => n,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,Set(CALLERID(name)=Customer Service:${CALLERID(name)})
 same => n,Answer()
 same => n,NoCDR()
 same => n,Dial(Local/102@T8_ring-group-dial/n,30,rcm(moh6)tTU(clean-variables))
 same => n,Dial(Local/111@T8_ring-group-dial/n,30,rcm(moh6)tTU(clean-variables))
 same => n,Dial(Local/101@T8_ring-group-dial/n,30,rcm(moh6)tTU(clean-variables))
 same => n,ResetCDR(ve)
 same => n,Set(__CALL_ORIGIN=normal)
 same => n,Set(__IGNORE_DIVERSIONS=no)
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Set(__NO_POST_SERVICES=FALSE)
 same => n,Set(__DISABLE_CF_AA=FALSE)
 same => n,Set(__SKIP_AA=FALSE)
 same => n,Set(__SKIP_BUSY=FALSE)
 same => n,Set(__RG_RINGTIME=)
 same => n,Goto(sub-extensions-vm,VM-101,1)

exten => 803,1,NoOp(Ring Group: Manager)
 same => n,Set(_IGNORE_DIVERSIONS=no)
 same => n,Set(_SKIP_PLAYBACK=TRUE)
 same => n,Set(__SKIP_AA=TRUE)
 same => n,Set(__NO_POST_SERVICES=TRUE)
 same => n,Set(__SRC_APP=RG803)
 same => n,Set(__PBX_APP=RING_GROUP)
 same => n,Set(__PBX_APP_DESC=Manager)
 same => n,Set(__RG_RINGTIME=30)
 same => n,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,Set(CALLERID(name)=Manager :${CALLERID(name)})
 same => n,Answer()
 same => n,NoCDR()
 same => n,Dial(Local/109@T8_ring-group-dial/n,30,rcm(moh6)tTU(clean-variables))
 same => n,ResetCDR(ve)
 same => n,Set(__CALL_ORIGIN=normal)
 same => n,Set(__IGNORE_DIVERSIONS=no)
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Set(__NO_POST_SERVICES=FALSE)
 same => n,Set(__DISABLE_CF_AA=FALSE)
 same => n,Set(__SKIP_AA=FALSE)
 same => n,Set(__SKIP_BUSY=FALSE)
 same => n,Set(__RG_RINGTIME=)
 same => n,Goto(sub-extensions-vm,VM-101,1)

exten => 901,1,NoOp(Ring Group: After Hours CS)
 same => n,Set(_IGNORE_DIVERSIONS=no)
 same => n,Set(_SKIP_PLAYBACK=TRUE)
 same => n,Set(__SKIP_AA=TRUE)
 same => n,Set(__NO_POST_SERVICES=TRUE)
 same => n,Set(__SRC_APP=RG901)
 same => n,Set(__PBX_APP=RING_GROUP)
 same => n,Set(__PBX_APP_DESC=After Hours CS)
 same => n,Set(__RG_RINGTIME=30)
 same => n,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,Set(CALLERID(name)=Customer Service:${CALLERID(name)})
 same => n,Answer()
 same => n,NoCDR()
 same => n,Dial(Local/101@T8_ring-group-dial/n&Local/105@T8_ring-group-dial/n&Local/106@T8_ring-group-dial/n&Local/104@T8_ring-group-dial/n,30,rcm(moh6)tTU(clean-variables))
 same => n,ResetCDR(ve)
 same => n,Set(__CALL_ORIGIN=normal)
 same => n,Set(__IGNORE_DIVERSIONS=no)
 same => n,Set(__SKIP_CONTACT_SERVICES=FALSE)
 same => n,Set(__NO_POST_SERVICES=FALSE)
 same => n,Set(__DISABLE_CF_AA=FALSE)
 same => n,Set(__SKIP_AA=FALSE)
 same => n,Set(__SKIP_BUSY=FALSE)
 same => n,Set(__RG_RINGTIME=)
 same => n,Goto(sub-extensions-vm,VM-102,1)

exten => i,1,Goto(invalid-dest,s,1)

[T8_queue-call-to-agents]
exten => _[-+*#0-9].,1,NoOp(More than on digit pattern)
 same => n,Gosub(s,1(${EXTEN}))

exten => _[-+*#0-9],1,NoOp(One Digit pattern)
 same => n,Gosub(s,1(${EXTEN}))

exten => s,1,Set(EXTENSION=${ARG1})
 same => n,Set(TENANT=106048d48cb4ddf6)
 same => n,Set(__SRC_APP=Q${QUEUE_NUMBER})
 same => n,Set(COS=${DB(${TENANT}/extensions/${EXTENSION}/context)})
 same => n,NoOp(Dialing Agent ${EXTENSION} from ${CALLERID(num)})
 same => n,Set(__DISABLE_CF_AA=TRUE)
 same => n,GotoIf($["${LEN(${COS})}"="0"]?end)
 same => n,Gosub(${COS},${EXTENSION},1)
 same => n(end),Hangup()

[T8_ext-queues]
exten => 750,1,NoOp(Queue: Phone Orders)
 same => n,Set(__QUEUE_UID=${UNIQUEID})
 same => n,Set(__QUEUE_CALL=TRUE)
 same => n,Set(__SKIP_AA=TRUE)
 same => n,Set(__FROM_QUEUE_ID=2)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__QUEUE_NUMBER=750)
 same => n,Set(__QUEUE_NAME=T8_Q750)
 same => n,Set(__PBX_APP=QUEUE)
 same => n,Set(__PBX_APP_DESC=Phone Orders)
 same => n,Set(__FORCE_QUEUE_MOH=no)
 same => n,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,Set(__INHERITED_PREFIX=Phone Orders)
 same => n,Set(__QUEUE_NO_CDR=TRUE)
 same => n(qconnect),NoOp(Connecting to Queue)
 same => n,Set(ANSWER_CHANNEL=yes)
 same => n,ExecIf($[$["${SKIP_ANSWER}"="yes"]|$["${ANSWER_CHANNEL}"="no"]]?Progress():Answer())
 same => n,NoCDR()
 same => n,Set(Q_RING_TIME=500)
 same => n,ExecIf($["${DISABLE_QRT}"="yes"]?Set(Q_RING_TIME=):)
 same => n,Queue(T8_Q750,cC,,,${Q_RING_TIME},,,,,${Q_FORCE_POSITION})
 same => n,ResetCDR(ve)
 same => n,NoOp(Queue Status: ${QUEUESTATUS})
 same => n,Set(__QUEUE_CALL=FALSE)
 same => n,GotoIf($["${QUEUESTATUS}"="CONTINUE"]?app-termination,hangup,1)
 same => n,Goto(sub-extensions-vm,VM-101,1)
 same => n,Hangup()

exten => 751,1,NoOp(Queue: Customer Service)
 same => n,Set(__QUEUE_UID=${UNIQUEID})
 same => n,Set(__QUEUE_CALL=TRUE)
 same => n,Set(__SKIP_AA=TRUE)
 same => n,Set(__FROM_QUEUE_ID=3)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__QUEUE_NUMBER=751)
 same => n,Set(__QUEUE_NAME=T8_Q751)
 same => n,Set(__PBX_APP=QUEUE)
 same => n,Set(__PBX_APP_DESC=Customer Service)
 same => n,Set(__FORCE_QUEUE_MOH=no)
 same => n,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,Set(__INHERITED_PREFIX=Customer Service)
 same => n,Set(__QUEUE_NO_CDR=TRUE)
 same => n(qconnect),NoOp(Connecting to Queue)
 same => n,Set(ANSWER_CHANNEL=yes)
 same => n,ExecIf($[$["${SKIP_ANSWER}"="yes"]|$["${ANSWER_CHANNEL}"="no"]]?Progress():Answer())
 same => n,NoCDR()
 same => n,Set(Q_RING_TIME=40)
 same => n,ExecIf($["${DISABLE_QRT}"="yes"]?Set(Q_RING_TIME=):)
 same => n,Queue(T8_Q751,cC,,,${Q_RING_TIME},,,,,${Q_FORCE_POSITION})
 same => n,ResetCDR(ve)
 same => n,NoOp(Queue Status: ${QUEUESTATUS})
 same => n,Set(__QUEUE_CALL=FALSE)
 same => n,GotoIf($["${QUEUESTATUS}"="CONTINUE"]?app-termination,hangup,1)
 same => n,Goto(sub-extensions-vm,VM-102,1)
 same => n,Hangup()

exten => 752,1,NoOp(Queue: After Hours CS)
 same => n,Set(__QUEUE_UID=${UNIQUEID})
 same => n,Set(__QUEUE_CALL=TRUE)
 same => n,Set(__SKIP_AA=TRUE)
 same => n,Set(__FROM_QUEUE_ID=4)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Set(__QUEUE_NUMBER=752)
 same => n,Set(__QUEUE_NAME=T8_Q752)
 same => n,Set(__PBX_APP=QUEUE)
 same => n,Set(__PBX_APP_DESC=After Hours CS)
 same => n,Set(__FORCE_QUEUE_MOH=no)
 same => n,Gosub(sub-set-call-vars,app-incoming,1)
 same => n,Set(__QUEUE_NO_CDR=TRUE)
 same => n(qconnect),NoOp(Connecting to Queue)
 same => n,Set(ANSWER_CHANNEL=yes)
 same => n,ExecIf($[$["${SKIP_ANSWER}"="yes"]|$["${ANSWER_CHANNEL}"="no"]]?Progress():Answer())
 same => n,NoCDR()
 same => n,Set(Q_RING_TIME=30)
 same => n,ExecIf($["${DISABLE_QRT}"="yes"]?Set(Q_RING_TIME=):)
 same => n,Queue(T8_Q752,cC,,,${Q_RING_TIME},,,,,${Q_FORCE_POSITION})
 same => n,ResetCDR(ve)
 same => n,NoOp(Queue Status: ${QUEUESTATUS})
 same => n,Set(__QUEUE_CALL=FALSE)
 same => n,GotoIf($["${QUEUESTATUS}"="CONTINUE"]?app-termination,hangup,1)
 same => n,Goto(sub-extensions-vm,VM-102,1)
 same => n,Hangup()

exten => h,1,NoOp(Ending Queue Call)
 same => n,Hangup()

[T8_ARS-all]
include => ARS-29


[ARS-9]
exten => i,1,ExecIf($["${FROM_QUEUE_CALLBACK}"="yes"]?Hangup():)
 same => n,Goto(invalid-dest,s,1)

[T8_default-trunk]
exten => _[+*#0-9A-Za-z].,1,Gosub(T8_set-global-tenant-vars,s,1)
 same => n,Gosub(sub-check-blacklist,s,1(106048d48cb4ddf6,${CALLERID(num)}))
 same => n,Gosub(sub-stir-shaken-verify,s,1(106048d48cb4ddf6,${CALLERID(num)}))
 same => n,Gosub(sub-setup-call-type,s,1(incoming))
 same => n,Gosub(dynamic-routing-in,s,1(${CALLERID(num)}))
 same => n,ExecIf($["${LEN(${DID_NUMBER})}"="0"]?Set(__DID_NUMBER=${EXTEN}):)
 same => n,Goto(T8_incoming-calls,${EXTEN},1)

[T8_incoming-calls]
exten => _[+*#0-9A-Za-z].,1,NoOp(INBOUND_ROUTE: Default)
 same => n,Set(CHANNEL(language)=en)
 same => n,Set(__INBOUND_LANGUAGE=en)
 same => n,Gosub(sub-set-call-vars,s-incoming,1(${CALLERID(num)},${EXTEN},106048d48cb4ddf6))
 same => n,Set(ICALL=yes)
 same => n,Goto(verify-did,${CALL_DESTINATION},1)
 same => n,Hangup()

exten => _8452449666,1,NoOp(INBOUND_ROUTE: Main)
 same => n,Set(CHANNEL(language)=en)
 same => n,Set(__INBOUND_LANGUAGE=en)
 same => n,Gosub(sub-set-moh,s,1(default,YES))
 same => n,Gosub(sub-set-call-vars,s-incoming,1(${CALLERID(num)},${EXTEN},106048d48cb4ddf6))
 same => n,Set(ICALL=yes)
 same => n,Goto(T8_app-ivr,IVR-23,1)
 same => n,Hangup()

exten => fax,1,NoOp(Fax Detected)
 same => n,Set(FAXOPT(faxdetect)=no)
 same => n,Goto(${FAX_DEST_CONT},${FAX_DEST_EXT},${FAX_DEST_PRIO})

exten => i,1,NoCDR()
 same => n,Goto(invalid-dest,s,1)

exten => pm-failed,1,NoCDR()
 same => n,Answer()
 same => n,Playback(sorry&vm-goodbye,skip)
 same => n,Hangup()

[T8_app-disa]
exten => DISA-15,1,NoOp(DISA: Main)
 same => n,Answer()
 same => n,Playback(vpbx/disa-prompt)
 same => n,Set(TIMEOUT(digit)=5)
 same => n,Set(TIMEOUT(response)=10)
 same => n,ExecIf($["${CALL_TYPE}"="2"]?Set(__EXT_CID_CONSTRUCTED=yes):)
 same => n,DISA(no-password,T8_cos-all,"Gesheft" <8452449666>)
 same => n,Hangup()

exten => i,1,NoCDR()
 same => n,Goto(invalid-dest,s,1)

exten => t,1,NoCDR()
 same => n,Goto(timeout-reached,s,1)

[T8_app-ivr]
exten => IVR-22,1,Goto(IVR-22,s,1)

exten => IVR-23,1,Goto(IVR-23,s,1)

exten => IVR-36,1,Goto(IVR-36,s,1)

[IVR-22]
exten => s,1,NoOp(IVR: Orders)
 same => n,Set(INVALIDATTEMPTS=0)
 same => n,Set(TIMEOUTATTEMPTS=0)
 same => n,Set(TIMEOUT(digit)=2)
 same => n,Set(TIMEOUT(response)=10)
 same => n,Set(__PBX_APP=QUEUE)
 same => n,Set(__PBX_APP_DESC=Orders)
 same => n,Answer()
 same => n(begin),NoOp(IVR Menu Begin)
 same => n(welcome-background),BackGround(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/a1d0c6e83f027327d8461063f4ac58a6)
 same => n(retry),NoOp(IVR Retry Section)
 same => n(retry-background),NoOp(No retry message)
 same => n,Set(CHANNEL(hangup_handler_push)=notify-call-hangup,s,1)
 same => n,WaitExten(10)

exten => 1,1,NoOp(Option 1 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "22" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_ext-queues,750,1)

exten => 2,1,NoOp(Option 2 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "22" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(sub-extensions-vm,VM-101,1)

exten => 3,1,NoOp(Option 3 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "22" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_app-announcement,announcement-9,1)

exten => 4,1,NoOp(Option 4 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "22" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_ext-ringgroups,801,1)

exten => #,1,Hangup()

exten => *,1,Goto(s,begin)

exten => t,1,Set(TIMEOUTATTEMPTS=$[${TIMEOUTATTEMPTS}+1])
 same => n,GotoIf($[${TIMEOUTATTEMPTS}>=3]?timeout)
 same => n,BackGround(option-is-invalid)
 same => n,Goto(s,begin)
 same => n(timeout),BackGround(sorry-youre-having-problems&vm-goodbye)
 same => n,Goto(sub-extensions-vm,VM-101,1)

exten => i,1,Set(INVALIDATTEMPTS=$[${INVALIDATTEMPTS}+1])
 same => n,GotoIf($[${INVALIDATTEMPTS}>=3]?invalid)
 same => n,ExecIf($["${INVALID_DIAL}"!="yes"]?BackGround(option-is-invalid):)
 same => n,Goto(s,begin)
 same => n(invalid),BackGround(sorry-youre-having-problems&vm-goodbye)
 same => n,Goto(sub-extensions-vm,VM-101,1)

exten => invalid_dial,1,NoCDR()
 same => n,NoOp(Invalid Numbering Dial)
 same => n,Playback(silence/1&no-route-exists-to-dest&vm-pls-try-again)
 same => n,Set(INVALID_DIAL=yes)
 same => n,Goto(i,1)

exten => h,1,NoOp(IVR-22 call ended)
 same => n,Hangup()

[IVR-23]
exten => s,1,NoOp(IVR: Main)
 same => n,Set(INVALIDATTEMPTS=0)
 same => n,Set(TIMEOUTATTEMPTS=0)
 same => n,Set(TIMEOUT(digit)=2)
 same => n,Set(TIMEOUT(response)=10)
 same => n,Set(__PBX_APP=QUEUE)
 same => n,Set(__PBX_APP_DESC=Main)
 same => n,Answer()
 same => n(begin),NoOp(IVR Menu Begin)
 same => n(welcome-background),NoOp(No welcome message)
 same => n(retry),NoOp(IVR Retry Section)
 same => n(retry-background),BackGround(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/14bfa6bb14875e45bba028a21ed38046)
 same => n,Set(CHANNEL(hangup_handler_push)=notify-call-hangup,s,1)
 same => n,WaitExten(10)

exten => 1,1,NoOp(Option 1 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_app-time-condition,TC-4,1)

exten => 2,1,NoOp(Option 2 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,898,1)

exten => 3,1,NoOp(Option 3 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_app-time-condition,TC-6,1)

exten => 4,1,NoOp(Option 4 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_app-announcement,announcement-22,1)

exten => 5,1,NoOp(Option 5 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,109,1)

exten => 6,1,NoOp(Option 6 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,112,1)

exten => 7,1,NoOp(Option 7 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,111,1)

exten => 101,1,NoOp(Option 101 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,101,1)

exten => 102,1,NoOp(Option 102 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,102,1)

exten => 103,1,NoOp(Option 103 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-103,103,1)

exten => 104,1,NoOp(Option 104 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,104,1)

exten => 105,1,NoOp(Option 105 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,105,1)

exten => 106,1,NoOp(Option 106 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,106,1)

exten => 107,1,NoOp(Option 107 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-103,107,1)

exten => 108,1,NoOp(Option 108 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,108,1)

exten => 109,1,NoOp(Option 109 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_ext-ringgroups,803,1)

exten => 112,1,NoOp(Option 112 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,112,1)

exten => 114,1,NoOp(Option 114 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,114,1)

exten => 111,1,NoOp(Option 111 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_cos-all,111,1)

exten => 13132,1,NoOp(Option 13132 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_ext-queues,750,1)

exten => 303,1,NoOp(Option 303 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_app-custom-application,303,1)

exten => 8,1,NoOp(Option 8 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_app-announcement,announcement-8,1)

exten => 9,1,NoOp(Option 9 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(sub-extensions-vm,VM-101,1)

exten => 0,1,NoOp(Option 0 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "23" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_app-announcement,announcement-35,1)

exten => #,1,Hangup()

exten => *,1,Goto(s,begin)

exten => t,1,Set(TIMEOUTATTEMPTS=$[${TIMEOUTATTEMPTS}+1])
 same => n,GotoIf($[${TIMEOUTATTEMPTS}>=3]?timeout)
 same => n,BackGround(option-is-invalid)
 same => n,Goto(s,begin)
 same => n(timeout),BackGround(sorry-youre-having-problems&vm-goodbye)
 same => n,Goto(T8_cos-all,102,1)

exten => i,1,Set(INVALIDATTEMPTS=$[${INVALIDATTEMPTS}+1])
 same => n,GotoIf($[${INVALIDATTEMPTS}>=3]?invalid)
 same => n,ExecIf($["${INVALID_DIAL}"!="yes"]?BackGround(option-is-invalid):)
 same => n,Goto(s,begin)
 same => n(invalid),BackGround(sorry-youre-having-problems&vm-goodbye)
 same => n,Goto(T8_cos-all,102,1)

exten => invalid_dial,1,NoCDR()
 same => n,NoOp(Invalid Numbering Dial)
 same => n,Playback(silence/1&no-route-exists-to-dest&vm-pls-try-again)
 same => n,Set(INVALID_DIAL=yes)
 same => n,Goto(i,1)

exten => h,1,NoOp(IVR-23 call ended)
 same => n,Hangup()

[IVR-36]
exten => s,1,NoOp(IVR: Specials)
 same => n,Set(INVALIDATTEMPTS=0)
 same => n,Set(TIMEOUTATTEMPTS=0)
 same => n,Set(TIMEOUT(digit)=2)
 same => n,Set(TIMEOUT(response)=10)
 same => n,Set(__PBX_APP=QUEUE)
 same => n,Set(__PBX_APP_DESC=Specials)
 same => n,Answer()
 same => n(begin),NoOp(IVR Menu Begin)
 same => n(welcome-background),BackGround(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/03afdbd66e7929b125f8597834fa83a4)
 same => n(retry),NoOp(IVR Retry Section)
 same => n(retry-background),NoOp(No retry message)
 same => n,Set(CHANNEL(hangup_handler_push)=notify-call-hangup,s,1)
 same => n,WaitExten(10)

exten => 1,1,NoOp(Option 1 has been pressed)
 same => n,System(/usr/share/vitalpbx/scripts/ivr_stats "36" "${EXTEN}" "${CALLERID(name)}" "${CALLERID(number)}" "${CALL_DESTINATION}" "${CDR(uniqueid)}")
 same => n,Set(__SRC_APP=IVR)
 same => n,Set(CALL_DESTINATION=${EXTEN})
 same => n,Goto(T8_app-ivr,IVR-22,1)

exten => #,1,Hangup()

exten => *,1,Goto(s,begin)

exten => t,1,Set(TIMEOUTATTEMPTS=$[${TIMEOUTATTEMPTS}+1])
 same => n,GotoIf($[${TIMEOUTATTEMPTS}>=3]?timeout)
 same => n,BackGround(option-is-invalid)
 same => n,Goto(s,begin)
 same => n(timeout),BackGround(sorry-youre-having-problems&vm-goodbye)
 same => n,Goto(T8_app-ivr,IVR-23,1)

exten => i,1,Set(INVALIDATTEMPTS=$[${INVALIDATTEMPTS}+1])
 same => n,GotoIf($[${INVALIDATTEMPTS}>=3]?invalid)
 same => n,Goto(s,begin)
 same => n(invalid),NoOp(All tries has done)
 same => n,Goto(T8_app-ivr,IVR-23,1)

exten => invalid_dial,1,NoCDR()
 same => n,NoOp(Invalid Numbering Dial)
 same => n,Playback(silence/1&no-route-exists-to-dest&vm-pls-try-again)
 same => n,Set(INVALID_DIAL=yes)
 same => n,Goto(i,1)

exten => h,1,NoOp(IVR-36 call ended)
 same => n,Hangup()

[T8_ivr-only-extensions]
exten => _[*#+0-9].,1,Set(__CALL_ORIGIN=RESTRICTED_IVR_CALL)
 same => n,Goto(T8_cos-all,${EXTEN},1)
 same => n,Hangup()

exten => h,1,NoOp(Ending DIRECT DIAL ON IVR)
 same => n,Hangup()

[TG-4]
exten => s,1,NoOp(Time Group: Store Hours)
 same => n,Set(__TGMATCH=0)
 same => n,Set(TG_TIMEZONE=${IF($[${LEN(${TC_TIMEZONE})}=0]?:/usr/share/zoneinfo/${TC_TIMEZONE})})
 same => n,GotoIfTime(07:00-22:30,mon-wed,*,*,${TG_TIMEZONE}?match:)
 same => n,GotoIfTime(07:00-23:00,wed-thu,*,*,${TG_TIMEZONE}?match:)
 same => n,GotoIfTime(07:00-23:59,thu-fri,*,*,${TG_TIMEZONE}?match:)
 same => n,GotoIfTime(07:00-12:00,fri-sat,*,*,${TG_TIMEZONE}?match:)
 same => n,Return()
 same => n(match),Set(__TGMATCH=1)
 same => n,Return()

[TG-5]
exten => s,1,NoOp(Time Group: Phone Order Hours)
 same => n,Set(__TGMATCH=0)
 same => n,Set(TG_TIMEZONE=${IF($[${LEN(${TC_TIMEZONE})}=0]?:/usr/share/zoneinfo/${TC_TIMEZONE})})
 same => n,GotoIfTime(09:00-18:00,sun-thu,*,*,${TG_TIMEZONE}?match:)
 same => n,GotoIfTime(09:00-13:00,fri,*,*,${TG_TIMEZONE}?match:)
 same => n,Return()
 same => n(match),Set(__TGMATCH=1)
 same => n,Return()

[T8_app-announcement]
exten => announcement-8,1,NoOp(Announcement: HOA)
 same => n,Playback(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/3416a75f4cea9109507cacd8e2f2aefc)
 same => n,Goto(T8_app-ivr,IVR-23,1)

exten => announcement-9,1,NoOp(Announcement: Phone Order Hours)
 same => n,Playback(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/17e62166fc8586dfa4d1bc0e1742c08b)
 same => n,Goto(T8_app-ivr,IVR-23,1)

exten => announcement-10,1,NoOp(Announcement: Phone Order After Hours)
 same => n,Playback(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/f7177163c833dff4b38fc8d2872f1ec6)
 same => n,Goto(sub-extensions-vm,VM-101,1)

exten => announcement-20,1,NoOp(Announcement: Specials )
 same => n,Playback(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/03afdbd66e7929b125f8597834fa83a4)
 same => n,Goto(T8_app-ivr,IVR-23,1)

exten => announcement-22,1,NoOp(Announcement: Specials Menu)
 same => n,Playback(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/fc490ca45c00b1249bbe3554a4fdf6fb)
 same => n,Goto(T8_app-ivr,IVR-36,1)

exten => announcement-26,1,NoOp(Announcement: Purim)
 same => n,Playback(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/d09bf41544a3365a46c9077ebb5e35c3)
 same => n,Goto(T8_app-announcement,announcement-10,1)

exten => announcement-34,1,NoOp(Announcement: PRESS 0)
 same => n,Playback(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/c7e1249ffc03eb9ded908c236bd1996d)
 same => n,Goto(T8_app-ivr,IVR-23,1)

exten => announcement-35,1,NoOp(Announcement: Pasach 2026)
 same => n,Playback(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/93db85ed909c13838ff95ccfa94cebd9)
 same => n,Goto(T8_app-ivr,IVR-23,1)

exten => announcement-39,1,NoOp(Announcement: Pesach phone phone orders)
 same => n,Playback(/var/lib/vitalpbx/static/106048d48cb4ddf6/recordings/92cc227532d17e56e07902b254dfad10)
 same => n,Goto(T8_app-ivr,IVR-23,1)

[T8_app-time-condition]
exten => TC-4,1,NoOp(Time Condition: Phone Orders)
 same => n,Set(TC_TIMEZONE=America/New_York)
 same => n,Gosub(TG-5,s,1)
 same => n,NoOp(${TGMATCH})
 same => n,Set(OVERRIDE_STATE=${DB(${TENANT}/time_conditions/TC4/override)})
 same => n,GotoIf($["${OVERRIDE_STATE}"!="no"]?:check-default)
 same => n,GotoIf($["${OVERRIDE_STATE}"="force_match"]?match)
 same => n,GotoIf($["${OVERRIDE_STATE}"="force_unmatch"]?unmatch)
 same => n(check-default),GotoIf($[${TGMATCH} > 0]?match)
 same => n(unmatch),NoOp(Time Condition No Matched)
 same => n,Goto(T8_app-announcement,announcement-10,1)
 same => n,Hangup()
 same => n(match),NoOp(Time Condition Matched)
 same => n,Goto(T8_app-ivr,IVR-22,1)
 same => n,Hangup()

exten => 900,1,NoOp(Time Condition: Phone Orders)
 same => n,Set(TC_TIMEZONE=America/New_York)
 same => n,Gosub(sub-toggle-tc-state,s,1(4,TG-5))
 same => n,Hangup()

exten => TC-6,1,NoOp(Time Condition: After Hours CS)
 same => n,Set(TC_TIMEZONE=America/New_York)
 same => n,Gosub(TG-5,s,1)
 same => n,NoOp(${TGMATCH})
 same => n,Set(OVERRIDE_STATE=${DB(${TENANT}/time_conditions/TC6/override)})
 same => n,GotoIf($["${OVERRIDE_STATE}"!="no"]?:check-default)
 same => n,GotoIf($["${OVERRIDE_STATE}"="force_match"]?match)
 same => n,GotoIf($["${OVERRIDE_STATE}"="force_unmatch"]?unmatch)
 same => n(check-default),GotoIf($[${TGMATCH} > 0]?match)
 same => n(unmatch),NoOp(Time Condition No Matched)
 same => n,Goto(T8_ext-queues,752,1)
 same => n,Hangup()
 same => n(match),NoOp(Time Condition Matched)
 same => n,Goto(T8_ext-queues,751,1)
 same => n,Hangup()

exten => 902,1,NoOp(Time Condition: After Hours CS)
 same => n,Set(TC_TIMEZONE=America/New_York)
 same => n,Gosub(sub-toggle-tc-state,s,1(6,TG-5))
 same => n,Hangup()

[QUEUE-CALLBACK-IVR-1]
exten => 1,1,Goto(QUEUE-CALLBACK-SC-1,s,1)
 same => n,Hangup()

[QUEUE-CALLBACK-SC-1]
exten => s-direct,1,NoOp(Setting up a direct queue callback)
 same => n,Set(_FROM_QUEUE_ID=2)
 same => n,Set(_QUEUEPOSITION=1)
 same => n,Goto(QUEUE-CALLBACK-SC-1,s,1)

exten => s,1,Set(CALLBACK_NUMBER=${CALLERID(num)})
 same => n,Set(ASK_NUMBER=yes)
 same => n,Set(TRIES=0)
 same => n,Set(MAX_TRIES=3)
 same => n(loop),Set(TRIES=$[${TRIES} + 1])
 same => n,GotoIf($[${TRIES}>${MAX_TRIES}]?max-retries,1)
 same => n,Read(CALLBACK_NUMBER,vpbx/qc-number-prompt,,,,)
 same => n,GotoIf($["X${CALLBACK_NUMBER}X"="XX"]?:check-cn)
 same => n,Goto(loop)
 same => n(check-cn),NoOp(Checking CallBack Number)
 same => n,Goto(${CALLBACK_NUMBER},1)
 same => n,Hangup()

exten => _1nxxnxxxxxx,1,SayDigits(${EXTEN})
 same => n,Read(CONFIRM,if-correct-press&digits/1&to-enter-a-diff-number&press&digits/0,1,,,5)
 same => n,GotoIf($["${CONFIRM}" = "1"]?:s,loop)
 same => n,NoOp(Scheduling callback to ${EXTEN} number)
 same => n,System(/usr/share/vitalpbx/scripts/queue_callback "schedule" "${TENANT}" "1" "${FROM_QUEUE_ID}" "${CALLERID(name)}" "${CALLBACK_NUMBER}" "${QUEUEPOSITION}")
 same => n,Playback(silence/1&vpbx/qc-thanks-prompt)
 same => n,Hangup()

exten => _nxxnxxxxxx,1,SayDigits(${EXTEN})
 same => n,Read(CONFIRM,if-correct-press&digits/1&to-enter-a-diff-number&press&digits/0,1,,,5)
 same => n,GotoIf($["${CONFIRM}" = "1"]?:s,loop)
 same => n,NoOp(Scheduling callback to ${EXTEN} number)
 same => n,System(/usr/share/vitalpbx/scripts/queue_callback "schedule" "${TENANT}" "1" "${FROM_QUEUE_ID}" "${CALLERID(name)}" "${CALLBACK_NUMBER}" "${QUEUEPOSITION}")
 same => n,Playback(silence/1&vpbx/qc-thanks-prompt)
 same => n,Hangup()

exten => max-retries,1,NoOp(Maximum Tries Reached)
 same => n,Playback(vpbx/vital-max-tries&please-try-again-later)
 same => n,Hangup()

exten => i,1,NoOp(Invalid Number)
 same => n,Playback(vpbx/qc-invalid-number)
 same => n,GotoIf($["${ASK_NUMBER}"="yes"]?s,loop)
 same => n,Hangup()

[QUEUE-CALLBACK-PC-1]
exten => s-out,1,NoCDR()
 same => n,Set(QCB_STATUS=${SHELL(/usr/share/vitalpbx/scripts/queue_callback "get_status" "${TENANT}" "${SQC_ID}")})
 same => n,GotoIf($["${QCB_STATUS}"="EXPIRED"]?end)
 same => n(perform-callback),Set(__FROM_QUEUE_CALLBACK=yes)
 same => n,NoOp(Calling to the customer)
 same => n,System(/usr/share/vitalpbx/scripts/queue_callback "set_status" "${TENANT}" "${SQC_ID}" "in_progress")
 same => n,NoOp(Calling to Number ${CALLBACK_NUMBER})
 same => n,Set(X=1)
 same => n,Set(MAX_TRIES=3)
 same => n,Set(CALLERID(all)="Gesheft" <8452449666>)
 same => n,Set(__EXT_CID_CONSTRUCTED=yes)
 same => n,Set(__SKIP_PIN_SETS=yes)
 same => n,Set(__O_RING_TIME=15)
 same => n,Set(CHANNEL(hangup_handler_push)=QUEUE-CALLBACK-PC-1,s-out,post-call)
 same => n,Goto(dial)
 same => n(retry),Set(Q_STATUS=retry)
 same => n,GotoIf($[${X}>${MAX_TRIES}]?max_retries,1)
 same => n,System(/usr/share/vitalpbx/scripts/queue_callback "set_status" "${TENANT}" "${SQC_ID}" "${Q_STATUS}")
 same => n(dial),Dial(Local/${CALLBACK_NUMBER}@T8_cos-all/n,15)
 same => n(post-call),GotoIf($["${DIALSTATUS}"="ANSWER"]?done)
 same => n,Wait(1)
 same => n,Set(X=$[${X} + 1])
 same => n,Goto(retry)
 same => n,Goto(end)
 same => n(done),System(/usr/share/vitalpbx/scripts/queue_callback "set_status" "${TENANT}" "${SQC_ID}" "successful")
 same => n(end),Hangup()

exten => max_retries,1,NoCDR()
 same => n,NoOp(Max Retries Reached)
 same => n,System(/usr/share/vitalpbx/scripts/queue_callback "set_status" "${TENANT}" "${SQC_ID}" "failed")

exten => s-in,1,NoCDR()
 same => n(perform-callback),Set(__SKIP_ANSWER=yes)
 same => n,NoOp(Calling to Queue ${QUEUE_NUMBER})
 same => n,Set(CALLERID(all)="${CALLBACK_NAME}" <${CALLBACK_NUMBER}>)
 same => n,Set(__DISABLE_QRT=yes)
 same => n,Set(__Q_FORCE_POSITION=${IF($["${QC_SAME_QUEUE}"="yes"]?${LAST_CALLER_POSITION})})
 same => n,Set(CHANNEL(hangup_handler_push)=QUEUE-CALLBACK-PC-1,s-in,post-call)
 same => n,Dial(Local/${QUEUE_NUMBER}@T8_cos-all/n)
 same => n(post-call),GotoIf($["${DIALSTATUS}"="ANSWER"]?end)
 same => n,System(/usr/share/vitalpbx/scripts/queue_callback "set_status" "${TENANT}" "${SQC_ID}" "failed")
 same => n(end),Hangup()

[T8_app-ai-assistants]
exten => h,1,NoOp(HangUp Virtual Assistant call)
 same => n,ExecIf($["X${AUDIO_RESPONSE}X"!="XX"]?System(rm -f ${AUDIO_RESPONSE}.wav):)
 same => n,ExecIf($["X${CALLER_RECORDING}X"!="XX"]?System(rm -f ${CALLER_RECORDING}):)
 same => n,Hangup()


