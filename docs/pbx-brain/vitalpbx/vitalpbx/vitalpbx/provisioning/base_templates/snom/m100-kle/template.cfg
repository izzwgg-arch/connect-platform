<?xml version="1.0" encoding="utf-8"?>
<settings>
    <sip_account>
        @foreach($accounts as $account)
        <idx id="{{ $loop->iteration }}">
            <sip_account_enable>1</sip_account_enable>
            <label>{{ $account['description'] ?? null }}</label>
            <display_name>{{ $account['description'] ?? null }}</display_name>
            <user_id>{{ $account['device_name'] ?? null }}</user_id>
            <authentication_name>{{ $account['device_name'] ?? null }}</authentication_name>
            <authentication_access_password>{{ $account['secret'] ?? null }}</authentication_access_password>
            <primary_sip_server_address>{{ $account['sip_domain'] ?? null }}</primary_sip_server_address>
            <primary_sip_server_port>{{ $account['sip_port'] ?? 5060 }}</primary_sip_server_port>
            <transport_mode>{{ $account['protocol'] ?? 'udp' }}</transport_mode>
        </idx>
        @endforeach
    </sip_account>

    <time_date>
        <date_format>{{ $dateFormat }}</date_format>
        <hr24_clock>{{ $timeFormat }}</hr24_clock>
        <selected_timezone>{{ $timezone }}</selected_timezone>
    </time_date>

    <user_pref>
        <web_language>{{ $language }}</web_language>
    </user_pref>

    <profile>
        <admin>
            <access_password>{{ $adminPassword }}</access_password>
        </admin>
    </profile>

    <provisioning>
        <firmware_url>{{ $connectionSettings['firmware_path'] ?? null }}</firmware_url>
    </provisioning>
</settings>
