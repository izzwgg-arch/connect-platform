{
  "screens": [
    { "screen": "notifications", "component": "NotificationsBody", "control": "notifications-markallread", "action": "click", "expected": "all unread notifications get readAt; unread badge clears", "permissions": "signed in", "api": "POST /notifications/read {all:true}", "testId": "notifications-markallread" },
    { "screen": "notifications", "component": "NotificationsBody", "control": "notifications-settings-link", "action": "click", "expected": "navigates to /settings/notifications", "permissions": "signed in", "api": "-", "testId": "notifications-settings-link" },
    { "screen": "notifications", "component": "NotificationsBody", "control": "notifications-filter-<key>", "action": "click", "expected": "list re-fetches scoped to the chosen class group (all/decisions/network/rfq/jobs/posts/events/security)", "permissions": "signed in", "api": "GET /notifications?filter=<key>", "testId": "notifications-filter-all | notifications-filter-decisions | notifications-filter-network | notifications-filter-rfq | notifications-filter-jobs | notifications-filter-posts | notifications-filter-events | notifications-filter-security" },
    { "screen": "notifications", "component": "NotifRow", "control": "notifications-accept-<id>", "action": "click", "expected": "connection request accepted, row removed from the list", "permissions": "signed in; row's kind === connection.request", "api": "POST /connections/:id/accept", "testId": "notifications-accept-<id>" },
    { "screen": "notifications", "component": "NotifRow", "control": "notifications-ignore-<id>", "action": "click", "expected": "connection request ignored, row removed from the list", "permissions": "signed in; row's kind === connection.request", "api": "POST /connections/:id/ignore", "testId": "notifications-ignore-<id>" },
    { "screen": "notifications", "component": "NotifRow", "control": "notifications-view-<id>", "action": "click", "expected": "navigates to the notification's href", "permissions": "signed in; row has an href", "api": "-", "testId": "notifications-view-<id>" },
    { "screen": "notifications", "component": "NotifRow", "control": "notifications-menu-<id>", "action": "click", "expected": "opens the row's overflow menu", "permissions": "signed in", "api": "-", "testId": "notifications-menu-<id>" },
    { "screen": "notifications", "component": "NotifRow", "control": "notifications-delete-<id>", "action": "click", "expected": "notification deleted, row removed from the list", "permissions": "signed in; owns the notification", "api": "DELETE /notifications/:id", "testId": "notifications-delete-<id>" },
    { "screen": "notifications", "component": "NotificationsBody", "control": "notifications-item-<id>", "action": "render", "expected": "one row per notification, grouped under Today/This week/Earlier, unread rows dot-marked, live inserts prepend on the 'notification' realtime event", "permissions": "signed in", "api": "GET /notifications", "testId": "notifications-item-<id>" },
    { "screen": "notifications", "component": "QuietHoursCard", "control": "notifications-edit-quiet-hours", "action": "click", "expected": "navigates to /settings/notifications", "permissions": "signed in", "api": "GET /me/notification-prefs (read-only summary)", "testId": "notifications-edit-quiet-hours" },
    { "screen": "notifications", "component": "ChannelsCard", "control": "notifications-edit-channels", "action": "click", "expected": "navigates to /settings/notifications", "permissions": "signed in", "api": "-", "testId": "notifications-edit-channels" },

    { "screen": "settings/notifications", "component": "NotificationSettingsPage", "control": "notifsettings-row-<kind>", "action": "render", "expected": "one row per notification class with In-app/Push/Email/SMS switches, SMS switch replaced by em-dash when the class is not security.alert", "permissions": "signed in", "api": "GET /me/notification-prefs", "testId": "notifsettings-row-<kind>" },
    { "screen": "settings/notifications", "component": "NotificationSettingsPage", "control": "inApp-<kind> | push-<kind> | email-<kind> | sms-<kind>", "action": "toggle", "expected": "flips the local switch state; persisted on Save", "permissions": "signed in; sms only on security.alert and only with a verified phone (400 phone_not_verified / sms_not_allowed otherwise)", "api": "-", "testId": "inApp-<kind>, push-<kind>, email-<kind>, sms-<kind>" },
    { "screen": "settings/notifications", "component": "NotificationSettingsPage", "control": "notifsettings-quiet-from / notifsettings-quiet-to", "action": "change", "expected": "updates the quiet-hours start/end time (HH:MM)", "permissions": "signed in; only editable while quiet hours are enabled", "api": "-", "testId": "notifsettings-quiet-from, notifsettings-quiet-to" },
    { "screen": "settings/notifications", "component": "NotificationSettingsPage", "control": "notifsettings-quiet-day-<0-6>", "action": "click", "expected": "toggles that weekday in quiet hours (0=Sun..6=Sat)", "permissions": "signed in", "api": "-", "testId": "notifsettings-quiet-day-0 .. notifsettings-quiet-day-6" },
    { "screen": "settings/notifications", "component": "NotificationSettingsPage", "control": "notifsettings-save", "action": "click", "expected": "saves class channels + quiet hours; re-reads and re-renders from the server", "permissions": "signed in", "api": "PUT /me/notification-prefs", "testId": "notifsettings-save" },

    { "screen": "shared", "component": "Uploader", "control": "<testId>-dropzone", "action": "click or drop", "expected": "opens the file picker (click) or accepts dropped files; uploads each to media", "permissions": "signed in", "api": "POST /media (multipart)", "testId": "<testId>-dropzone (default uploader-dropzone)" },
    { "screen": "shared", "component": "Uploader", "control": "<testId>-input", "action": "change", "expected": "hidden native file input backing the dropzone", "permissions": "signed in", "api": "POST /media (multipart)", "testId": "<testId>-input" },
    { "screen": "shared", "component": "Uploader", "control": "<testId>-remove", "action": "click", "expected": "removes the file from the list; deletes the uploaded asset if it had finished uploading", "permissions": "signed in; owns the asset", "api": "DELETE /media/:id", "testId": "<testId>-remove" },
    { "screen": "shared", "component": "Uploader", "control": "<testId>-alt", "action": "change", "expected": "sets alt text carried alongside the uploaded image asset in onChange", "permissions": "signed in", "api": "-", "testId": "<testId>-alt" },
    { "screen": "shared", "component": "MediaGallery", "control": "<testId>-cell-<i>", "action": "click", "expected": "opens the lightbox at that image/video; Escape closes, arrow keys navigate", "permissions": "public (renders whatever assets it is given)", "api": "GET /media/file/:id/:variant", "testId": "<testId>-cell-0 .. -3" }
  ]
}
