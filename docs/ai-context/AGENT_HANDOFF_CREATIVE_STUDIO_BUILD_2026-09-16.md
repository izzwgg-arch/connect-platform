# ⛔⛔ AGENT HANDOFF — CREATIVE STUDIO IS BUILT AND LIVE: images and AI video really generate, the Coworker has 10 `creative_*` tools, and the learning loop is proven end to end (2026-09-16)

Supersedes the Phase-0 design handoff (`AGENT_HANDOFF_CREATIVE_STUDIO_2026-09-15.md`), which is still the
right place for the mockup, the screen inventory and the licence research. **This file is what exists.**

Read before touching `apps/api/src/creativeStudio/`, the `/creative` routes, the `creative_*` agent tools,
or anything that generates media.

---

## 1. WHAT IS LIVE, AND PROVEN ON PRODUCTION

Deployed: **api `8a914d26`** (container-verified, healthy), migration `20260916140000_creative_studio`
applied (19 new tables). Portal pages and the agent tools are covered in §7.

Everything below was run against **production** on 2026-09-16, not a test harness:

| Proof | Result |
|---|---|
| Image generation | job queued → succeeded in **15s**; 2,022,197-byte PNG, 1024×1536 (the 4:5 that was asked for), thumbnail generated by FFmpeg |
| Video generation | 4-second shot: **74s**, 1,542,300 bytes, h264+aac, 1280×720, duration 4.1s |
| **A 15-second shot from a 12-second engine** | planned as **12 + 4**, two provider jobs, part 2 started from part 1's last frame, joined and trimmed → **duration exactly 15.000000s**, 3,900,780 bytes. This is the headline capability and it works. |
| Trademark refusal | "put the Coca-Cola logo on our van" → **422 refused**, with a sentence to pass to the customer |
| Wrong internal secret | **403** |
| Another company asking for the same job | **404** (not 403 — no existence leak); their asset list came back empty |
| Tampered signed URL | **401**; the correct one served exactly 2,022,197 bytes as `image/png` |
| **The learning loop** | 3 identical rejections → `suggested(1)`, `suggested(2)`, **`active(3)`**; the next generation returned `appliedMemory:["Prefers a slower pace"]` and the prompt actually sent contained *"Pacing: slower, let shots breathe."* |
| **A real Coworker turn** | agent chat, gpt-5, real user identity: the model chose the tools itself, created a project *"Website delivery photo"*, generated the image and replied *"All set — your image is ready and saved in Creative Studio…"* with sensible next steps |
| Stress: ten identical requests at once | **exactly one job created**, one job id returned |
| Stress: five different requests at once | some accepted, the rest refused with *"You already have 2 jobs running…"*; queue drained to **11 succeeded, 0 stuck** — while the api container was being replaced mid-flight |
| Worker death | a live job forced to *claimed by dead-worker, lease expired 5 minutes ago* was swept, re-claimed, ran and **succeeded** in ~20s |

⏳ **NOT yet proven:** no human has clicked the portal screens in a browser (all nine routes serve 200 and
the shipped bundle carries every permission key, but that is not the same as somebody using them), and no
customer has been granted a key.

## 2. THE FACTS THAT MADE THIS BUILDABLE TODAY (all verified 2026-09-16)

⛔ These are the load-bearing discoveries. The Phase-0 plan assumed we would need a new provider account,
object storage and a GPU. **None of that was true.**

- **The OpenAI key already in `AgentSecret` can do images AND video.** `gpt-image-2.5-flare`,
  `gpt-image-2`, `gpt-image-1.x` and **`sora-2` / `sora-2-pro`** are all on the account. No new provider,
  no new spend commitment.
- ⛔⛔ **Sora's real limits, read from its own validation errors:** `seconds` ∈ **{4, 8, 12}** only, `size` ∈
  {720x1280, 1280x720, 1024x1792, 1792x1024}. **There is no 15.** That is why `planVideoSegments()` exists
  and why a 15-second ask is composed — do not "simplify" it away.
- **`connectcomms-minio` was already running on `infra_default`, completely unused** (no buckets). api and
  worker are both on that network and reach it (health 200). That is the object storage; R2 was not needed.
- **FFmpeg 5.1.9 is in the api and worker images**, so joining, trimming, thumbnails, captions and
  re-framing cost nothing per run.
- ⛔ The api/agent/worker containers hold a **placeholder** `OPENAI_API_KEY` (`"(paste y…key)"`). The real
  keys live encrypted in `AgentSecret` (AES-256-GCM, `CREDENTIALS_MASTER_KEY`) and are read through
  `resolveCreativeSecret()`, which copies `voice/elevenLabsKey.ts`. **Never "fix" the env placeholder.**

## 3. SHAPE

```
portal /creative/*  ─┐
                     ├─→ apps/api/src/creativeStudio/service.ts  ← ONE implementation
Coworker tools ──────┘        (safety → engine choice → quota → brand kit + memory → job)
                                  │
                     jobs.ts (the job ROW is the truth; claim + lease + heartbeat)
                                  │
              engines.ts (adapters) ── openai images / sora video / elevenlabs voice+music
              localJobs.ts (ours)   ── export, timeline render  → media.ts (FFmpeg)
                                  │
                     assets.ts → storage.ts → MinIO   +   CreativeAsset rows
```

- **`service.ts` is the only way to start a generation.** The browser and the agent both go through it, so
  asking the Coworker can never skip the safety check, the quota check or the brand kit. ⛔ Do not add a
  second path.
- **`runCreativeCycle()` runs inside the api** on a 5-second timer (`CREATIVE_CYCLE_INTERVAL_MS`),
  claiming at most 2 jobs. It is written as a worker loop so it can be moved to `apps/worker` unchanged
  when volume justifies it.
- **Job states:** queued → claimed → running → succeeded / failed / cancelled. A provider-side job keeps
  its `providerJobId` and is polled; a lease that expires is swept back to the queue **unless** it has a
  provider job, in which case it goes back to polling rather than being paid for twice.

## 4. THE RULES THAT ARE CODE, NOT COMMENTS

- ⛔ **Licence gate.** `CreativeEngine.commercialOk = false` → `chooseEngine()` will not return it and
  `PATCH /admin/creative/engines/:id {enabled:true}` answers **422**. That is the enforcement of the whole
  Phase-0 licence finding.
- ⛔ **Company from the session only.** Every read is `findFirst({ where: { id, tenantId } })` and answers
  **not found**, never forbidden. Storage keys begin `t/<tenantId>/`, so a cross-company read cannot even
  be expressed.
- ⛔ **Money asks first.** `checkQuota()` runs before any provider call. Cancelling records the part-done
  work as spent (`wasted: true`) rather than hiding it.
- ⛔ **Idempotency.** `tenantId + idempotencyKey` is unique; the same request twice returns the same job.
- ⛔ **FFmpeg** is `execFile` with an argument array and `-protocol_whitelist file,pipe` on **every** call —
  a crafted playlist would otherwise make our own server fetch URLs.
- ⛔ **Uploads** are sniffed from their bytes (`sniffMime`), not their filename, and only an allowlisted
  type is stored.
- ⛔ **No tool publishes anything.** Export produces a file; a person sends it. A test asserts no
  `creative_(post|publish|send|email|share)_*` tool exists.

## 5. TWO BUGS THE TESTS CAUGHT (both were real)

1. **A run of dots survived `safeSegment`**, so `../../etc/passwd` became `_.._.._etc_passwd` — a key still
   containing `..`. Harmless in S3, dangerous the day anybody joins a key onto a filesystem path. Fixed by
   collapsing dot runs.
2. **`cancelJob()` read `job.status` AFTER its own update.** With Prisma's detached rows it worked; with a
   client that returns the same object it silently skipped recording the spend. Now the state is captured
   before the write. ⛔ General lesson: never re-read a local variable across your own mutation.

## 6. THE MISTAKE I MADE, AND THE ONE ANOTHER SESSION CAUGHT

- ⛔⛔ **`/internal/agent/creative/*` answered `unauthorized` on the first production call.** The handler
  checks the shared secret, but **the JWT preHandler rejects the request before the handler runs**.
  `jwtPublicRouteBypass.ts` says so in its own comment — *"Any new /internal/agent/* door must be added
  here too"* — and I did not read it carefully enough. Fixed in `8a914d26`; a test now pins it.
- ⛔⛔ **A patch of mine anchored on `} from "lucide-react";`, which ALSO matches inside
  `import type { LucideIcon } from "lucide-react";` on line 1 of navConfig.** The rebuild merged the two
  imports and nothing in the portal typechecked from origin. **Another session (connect-2-8f) caught it and
  repaired it in `88682602`.** A test now pins navConfig's import shape. General lesson for this shared
  tree: when re-applying an edit onto origin, anchor on something that appears **once**.

## 7. WHAT IS NOT DONE

- ✅ **Portal deployed** (`6ff2f685`): `/creative`, `/creative/images|video|assets|brand|memory|projects|
  design` and `/admin/creative-console` all serve **200**, and the shipped chunks carry all eight
  `can_view_creative_*` keys. ⛔ The first attempt refused with `HEAVY JOB ALREADY RUNNING:
  deploy-queue:portal:compose-build-portal` — another session was mid-build; that is a wait, not a fault.
- ✅ **Agent rebuilt** (compose profile:
  `docker compose -f docker-compose.app.yml -f docker-compose.agent.yml up -d --build agent`), and a real
  chat turn used the tools. ⛔ The FIRST turn replied *"ran out of investigation steps"* — the model spent
  its whole tool budget polling. Fixed: `creative_check_job` now waits up to 20s server-side and the
  prompt bounds it to two checks per reply. Re-tested: a proper answer.
- ⏳ **No permission key is granted to anybody.** Every key is in no default bucket: granting them IS the
  launch. Nine view keys + seven action keys, listed in §8.
- ⛔ **MinIO is being used with its ROOT credentials** (added to `/opt/connectcomms/env/.env.platform` as
  `CREATIVE_S3_*`). Acceptable today because that MinIO is otherwise completely unused, but the right thing
  is a service account scoped to the `creative` bucket. There is no `mc` binary on the box.
- ⏳ Not built yet from the approved mockups: the storyboard screen, the timeline editor UI (the RENDER
  side exists in `localJobs.ts`), voice/music/captions screens, export screen, agent-design-mode screen,
  and the self-evaluation pass (`creative_result_evaluate` is designed, not implemented).

## 8. PERMISSION KEYS (all in no default bucket)

Section `can_view_section_creative`; pages `can_view_creative_home|projects|images|video|design|assets|
brand_kit|memory`; console `can_view_admin_creative_console` (plus a SUPER_ADMIN force line).
Actions: `can_creative_generate_image`, `can_creative_generate_video`, `can_creative_use_premium`,
`can_manage_creative_brand_kit`, `can_creative_export`, `can_view_company_creative_projects`,
`can_delete_creative_projects`.

## 9. TESTS

`apps/api/src/creativeStudio/creativeStudio.test.ts` — **40 tests, all passing**, registered in
`apps/api/package.json` (a test asserts its own glob is registered). Three layers: pure logic (segment
planning, safety, prompt building, storage keys, captions), the job engine against an in-memory database
(idempotency, the licence gate, quota, single-winner claiming, lease recovery, cancel accounting,
cross-company refusal), and source guards (route registration, permission rules, the JWT bypass, the
navConfig import shape, no-publish tools, FFmpeg protocol whitelist).

⛔ Run them with `cd apps/api && node --experimental-test-module-mocks --import tsx --test
"src/creativeStudio/*.test.ts"`. The repo-wide api suite has **pre-existing** failures in other areas
(billing tests, delivery, mfa, `apiRequestProfiler`) that have nothing to do with this work.

---

# ⛔⛔ ROUND TWO — THE FILM PIPELINE (2026-09-16, later the same day)

§1–§9 above describe generation. This part describes everything that turns generated shots into a finished
film, and the six bugs that only appeared when it was actually run.

## 10. WHAT EXISTS NOW

```
storyboard → render each shot → assemble → voice, music, captions → the cut → export
```

| Piece | Where |
|---|---|
| Storyboard page | `apps/portal/app/(platform)/creative/storyboard/page.tsx` |
| Video editor | `.../creative/timeline/page.tsx` |
| Voice, music & captions | `.../creative/audio/page.tsx` (+ `apps/portal/lib/creativeCaptions.ts`) |
| Export | `.../creative/export/page.tsx` |
| The one editing channel | `.../creative/useProjectDoc.ts` — load, ops, 409 handling |
| Splitting + assembling | `apps/api/src/creativeStudio/film.ts` |
| Looking at the result | `apps/api/src/creativeStudio/evaluate.ts` |

New doors: `POST /creative/projects/:id/assemble`, `POST /creative/projects/:id/render`,
`POST /creative/export`, `GET /creative/export-presets`, `GET /creative/voices`, and the matching
`/internal/agent/creative/{storyboard,assemble,render,export}`.

New agent tools: `creative_write_storyboard`, `creative_assemble_film`, `creative_render_film`,
`creative_export_file`, plus `shot_id` on `creative_make_video`. ⛔ Still nothing that posts, sends or
publishes — a test asserts no such tool exists.

New keys, all in no default bucket: `can_view_creative_storyboard|timeline|audio|export`.

## 11. THE RULES THIS ROUND ADDED, AND WHY EACH ONE EXISTS

- ⛔ **One implementation per thing, used by both doors.** `assembleFilm()` is called by the button and by
  the agent; `storyboardDoc()` decides the split for both. The portal does NOT assemble the cut itself — a
  test asserts that, because a second implementation is the one nobody tests and the one a customer gets.
- ⛔ **A rendered clip attaches ITSELF to its shot** (`attachToShot` in `jobs.ts`, driven by
  `request.shotId`). It used to be a second step the model had to remember; the once it forgets, the
  storyboard says "not rendered" beside a clip the customer has paid for.
- ⛔ **The job stays `running` while it is being checked**, with its lease extended. A separate
  `"evaluating"` status would be invisible to `sweepLostLeases()`, so a crash mid-check would strand the job
  for ever.
- ⛔ **A checker that cannot answer PASSES the work.** Never hold a good picture hostage to our own check.
- ⛔ **Money asks first, still.** The monthly allowance is checked before anything is queued, so queueing is
  not a way around it.

## 12. THE SIX BUGS — every one found by running it, not by reading it

1. **The check was looking at nothing.** Its stored verdict read
   `{"ok":true,"checked":false,"note":"the checker could not answer"}` after 11 seconds. **Reasoning tokens
   count against `max_completion_tokens`**, which was 400: the model spent the whole budget thinking and
   returned empty content. 2000 on `gpt-5-mini` now; the next generation came back `"checked":true` in 7.5s.
   ⛔ A test fails if the budget drops below 1500.
2. **A 5-second shot cost two renders and a join.** `planVideoSegments(5)` planned 4+4. One call is used
   wherever one will do (`5 → [8]`, `9 → [12]`, `15 → [12,4]` as before). And the storyboard's default beat
   was 5 seconds, which Sora BILLS as 8 — it is **4** now, the engine's own smallest clip, so a 15-second
   film costs 16 seconds of engine time instead of 24.
3. **Every video was stored twice** — a raw `clip.mp4` and a re-encoded `shot.mp4` 20 KB bigger — because
   the single-segment shortcut compared exactly and a 4.1s clip for a 4s ask fell through to the join path.
   Half a second of tolerance, the piece is promoted in place, and raw pieces are `source: "segment"`:
   hidden from the library, the project view and the Coworker's own listing, swept after a week. Verified:
   **one 4-second shot now produces exactly one file.**
4. **"Render all four shots" rendered two, and the Coworker said the rest were coming.** The concurrency cap
   counted QUEUED work, so 3 and 4 were refused — and the model replied *"As soon as those finish, Shots 3
   and 4 will start automatically."* Nothing would ever have started them. ⛔ **A cap that makes the queue
   lie is worse than no cap.** It limits what is RUNNING; the rest wait and the runner drains two at a time,
   which is what the model was already telling people.
5. **Re-writing a storyboard threw away clips already paid for.** The document was replaced with fresh ids;
   two rendered shots were orphaned and the project read "0 of 4 rendered". `mergeStoryboard()` keeps the id
   and the clip of any shot whose description is unchanged (whitespace and case do not count), drops the
   clip of one that now says something else, and reports `keptClips` / `droppedClips`.
6. **A provider's real reason was thrown away.** A voiceover came back as "The voice engine refused (401)"
   while the body said *"Your subscription has a failed or incomplete payment."* `errorFrom` only read
   OpenAI's `error.message`; it reads ElevenLabs' `detail.message` too now. A problem with **our** account
   becomes *"not something you did, and we have been told"* and is **permanent** — retrying spends three
   attempts on the same wall.

⛔ A seventh, which is model behaviour rather than a defect: asked to change a shot, the Coworker created a
**second project with the same name** and left the rendered clips in the first. Creating a project with a
title and kind that already exist **today** now hands back the existing one with `reused: true`.

## 13. THE ELEVENLABS FINDING — NOT A CODE PROBLEM, AND IZZY HAS TO FIX IT

⛔⛔ The platform's ElevenLabs **subscription has a failed or incomplete payment**. Proven from inside the
api container: the key decrypts fine, `/v1/user/subscription` answers **200** (Creator tier, 49,878 of
1,036,000 characters used) and `/v1/voices` lists **38 voices** — but **every** `/v1/text-to-speech/…` call
answers **401 `payment_issue`**. So Creative Studio voiceovers and music cannot work, and anything else on
the platform that synthesises speech through ElevenLabs is in the same position until that invoice is paid.

## 14. PROVEN ON PRODUCTION

- **By hand, through the agent's own doors:** project → storyboard (12s → three 4-second shots, the studio
  doing the split) → three shots rendered, each clip attaching itself → assemble (3 clips, 0 skipped) → two
  captions through the ops door → **a stale write refused** with the current document → the cut rendered by
  FFmpeg in 16 seconds: **`final-cut.mp4`, 1280×720, exactly 12,000 ms**, plus `captions.srt` → exported to
  1920×1080, 1080×1920 and 1080×1080, all three succeeded.
- **By the Coworker, in a real chat:** it wrote a four-shot storyboard (`updatedByType: coworker`) and
  started **zero jobs** until asked; quoted the cost and the remaining allowance; rendered; and when told
  two clips were missing it assembled *"2 used, 2 skipped"* and rendered **1080×1920, exactly 6,000 ms** —
  $1.20 of engine spend.
- **The pages:** all twelve `/creative/*` routes 200; all four new keys in the shipped bundle; api, portal
  and agent containers at 0 restarts.

⛔ **My own mistake, for the record:** a shell loop written as a `for` over `$(cat file)` split on
WHITESPACE, so it fired one render per WORD — five jobs for three shots, about **$1.60 wasted**, and the
last one had to be cancelled through the real door. Read lines with `while IFS='|' read -r`.

## 15. WHAT IS STILL NOT DONE

- ⏳ **Nobody has opened these screens in a browser.** 200s and bundle greps are not a person using them.
- ⏳ **No key is granted to anybody.** Granting IS the launch. ⛔ Grant them as a SET: the shared calls sit
  under the `/creative` catch-all, which asks for `can_view_section_creative`.
- ⏳ The reject-and-re-render path is proven by test (8 cases in `creativeRetry.test.ts`), not yet by a real
  generator producing a real six-fingered hand.
- ⏳ MinIO is still on root credentials.
- ⏳ The agent-design-mode screen from the mockup is still unbuilt.
- ⚠️ `storyboardDoc` honours the shot count the model chooses, so four shots for a 12-second film gives 3s
  beats — billed as 4s each. Not wrong, but a model that picks shot counts fitting 4/8/12 spends less.
