# ⛔⛔ AGENT HANDOFF — GESHEFT (845) 244-9666 → TELNYX WITH ZERO DOWNTIME (2026-09-18) — read before touching any Telnyx port, the Telnyx webhook doors, or platform texting

Summary file: `docs/ai-context/claude-md-sections/2026-09-18-gesheft-port-to-telnyx-zero-downtime.md`.
Sibling handoffs read first: `AGENT_HANDOFF_TELNYX_ONBOARDING_2026-09-15.md` §15–16 (FastPort, the 723-1213 port, LOA/invoice recipe), `AGENT_HANDOFF_TELNYX_10DLC_2026-09-16.md` (Gesheft campaign C5GLU0S), `AGENT_HANDOFF_CARRIER_MIGRATION_2026-09-10.md` (the three lanes), memory `texting-switcher-voipms-to-telnyx`.

## 1. What today's 723-1213 port taught (the rehearsal)
- Order 58969290: `foc_datetime_requested 2026-09-18T11:00Z`, `foc_datetime_actual 2026-09-18T11:00Z`, order `updated_at 11:04:24Z`, status `ported`. Number `+18457231213` on the account `active`, connection 3049417907428656858, messaging profile 4001a0ab-3699-450e-83b9-77ac48c3f0dc — exactly the `phone_number_configuration` the filer put on the order, so routing existed the instant it landed.
- `TenantSmsNumber +18457231213` VOIPMS→TELNYX at 11:05:53Z (`telnyx.texting_switched` audit) — the switcher's 5-min loop. ⛔ The switcher hits Telnyx rate limits every pass (17 numbers checked one by one; 1–2 "rate-limiting" errors per run, rotating) — a number can miss a pass; it still flips within ~10 min. Worth batching later.
- No inbound CALLS to 723-1213 before or after (texting number) → inbound voice for a PORTED number on trunk 183 has NO real-call proof yet. Main's `_8457231213` dispatch is the same shape as `_8452449666`.
- ⛔⛔ Platform texting died at FOC: `resolveBillingSmsSender()` → VoIP.ms `sendSMS` from 8457231213 → `invalid_did` (probe 16:05Z). `BILLING_SMS_FROM_NUMBER=8457231213` in the api env. Candidates: 8453050012 (routing 344022_lcdemo2, sms 1) and 8455577768 (344022_lanhome, sms 1, the escalation FROM number). Env-only api change needs a carrying commit; the WORKER sends too (see memory `env-only-api-change-needs-a-carrying-commit`).

## 2. The webhook-door defect (fixed `9a27c130`)
- Symptom: `AgentAuditLog` `telnyx.webhook_refused {reason:"unsigned"}` ×16 from 09-16 16:06Z to 09-18 16:06Z; api log `POST /webhooks/telnyx/sms → 401`. A curl with fake signature headers ALSO said `unsigned` → `req.rawBody == null`.
- Cause: `app.register(fastifyRawBody, { global:false, … })` (server.ts:462) is deferred to `ready()`; the plugin adds capture through an `onRoute` hook, so only routes registered after it loads get it. Every route in server.ts registers synchronously before. The comment 5 lines above it records the same ordering trap for the rate limiter (solved there with `app.after()`); the raw-body line was never verified live. `/webhooks/whatsapp/meta` (server.ts:39803) uses the same `config:{rawBody:true}` and is presumably equally blind — NOT changed here.
- Fix: `apps/api/src/telnyx/rawBodyCapture.ts` — `captureRawBodyPreParsing` buffers the stream, sets `req.rawBody`, returns `Readable.from([buf])` with `receivedEncodedLength`; `SIGNED_WEBHOOK_ROUTE_OPTIONS = { config:{rawBody:true}, preParsing }` on both Telnyx doors. `rawBodyCapture.test.ts` boots a real Fastify in server.ts's order and injects real requests (3/4 fail on HEAD). `loopcomMobile.test.ts` guard updated to the new shape. Deployed 16:30Z after another session's lockfile-repair build released the heavy-job lock (first attempt: HEAVY JOB ALREADY RUNNING).
- Post-fix probes (all Loopcom-owned numbers, thread `cmspgrv8lwaovn0217h6uci3k` 723-1213↔557-7768 and its mirror):
  - B: 557-7768 (VoIP.ms) → 723-1213 (Telnyx): `telnyx.inbound_sms` 16:33:16Z, chat INBOUND `telnyx:4286d208-…`, 2 s.
  - C: VoIP.ms `sendMMS` media1=apple-icon.png → 723-1213: chat IMAGE `telnyx:3e68ef89-…`, attachment image/png 9,684 B (Telnyx re-encoded from 13,058), `mediaKind file` (api mirror omits it; worker path sets it from MIME — both clients infer from MIME so it renders inline anyway).
  - D: `/internal/chat/sms-system-reply` (x-cdr-secret) from the 723-1213 thread → worker → Telnyx `telnyx:4031a0b5-623d-…` `delivered` (DLR via the fixed door) → arrived on 557-7768 as `voipms:111584383` 6 s later. **The sender has NO 10DLC campaign** — Telnyx accepted it.
  - E: direct `POST /v2/messages` from +18457231213 with `media_urls=[signed download of Connect's own voice-note-1785432087145.m4a]` → `type MMS`, `telnyx.sms_status delivered` 16:42:23Z → VoIP.ms inbound `voipms:10336436` audio/mp4 **9,132 B = identical**, `media.m4a`. ⛔ Signed links: `buildChatDbSignedDownloadUrl` lives at `@connect/shared/chatSignedUrl` (node:crypto; not on the index), public base `https://app.connectcomunications.com/api`.

## 3. Gesheft inventory (read 2026-09-18)
- VoIP.ms `getDIDsInfo 8452449666`: routing `account:344022_gesheft`, pop 54, sms 1, e911 1, **port_out_pin 9593**, cnam 1, `sms_forward 8456623885`. `e911Info`: Gesheft, 33 Dinev Rd, Kiryas Joel NY 10950, email izzy@loopcom.net. Telnyx `POST /addresses/actions/validate` says VALID for Kiryas Joel / Monroe / Palm Tree spellings (keep Kiryas Joel = the VoIP.ms record).
- Telnyx `portability_checks`: portable, fast_portable, messaging_capable, carrier BANDWIDTH.COM CLEC, LLC - NY.
- Connect: `TenantSmsNumber` VOIPMS, voipmsAccountId default, isTenantDefault; chat 60 d: 1,555 INBOUND TEXT, 27 INBOUND IMAGE (24 attachments), 4 OUTBOUND; `SmsMessage` from 2449666: 0; `DeliverySmsConsent`: 0. Users: ap@/scn@/connect@/contact@gesheftkosher.com, orders@pileupny.com, yisraelweinstock@gmail.com — none carries a phone, so 845-662-3885 is unidentified (ask Izzy).
- PBX (read-only): `dialplan show 8452449666@default-trunk` → `_8452449666 … NoOp(Forwarding call to Gesheft tenant)` (extensions__50-1-dialplan.conf:14997). Trunk-183 inbound converges there (memory `carrier-migration-three-lanes`).
- 10DLC: campaign `4b3001a0-b4fc-4f42-cc9c-f2c4d1af057f` / TCR C5GLU0S, `MNO_PENDING` with operationStatus all seven APPROVED. `POST /10dlc/phone_number_campaigns {+18452449666}` → 404 "+18452449666 was not found" — pre-port attach is impossible; the engine's `advanceNumbers` GETs the assignment first, so a later assignment is tolerated.

## 4. The plan (what runs at FOC, lane by lane)
1. Inbound voice: carrier flips → trunk 183 → Main default-trunk → `_8452449666` → Gesheft. No PBX write. ⏳ needs one real call to a ported number (ask Izzy to call 723-1213 today).
2. Inbound SMS/MMS: order `phone_number_configuration.messaging_profile_id` = the sign-ups profile (webhook `/api/webhooks/telnyx/sms`) → door (fixed) → shared ingest. 0 gap.
3. Outbound SMS: switcher flips the row (≤5–10 min) → engine attaches to C5GLU0S (≤10 min) → Telnyx ASSIGNED (~2 h). Unregistered sends are accepted meanwhile (probe D). Exposure ≈ 1 text/month.
4. 911: create Telnyx address (33 Dinev Rd, Kiryas Joel) → `phone_number_configuration.emergency_address_id` on the order → active at FOC. ⛔ Requires Izzy to accept Telnyx's Emergency Terms of Service first (10015 otherwise) — until then the number has NO 911 after FOC.
5. VoIP.ms `sms_forward` to 845-662-3885 ends at FOC. Connect has no cell-forward; replacement = that person as a Connect user on the shared inbox (push), or nothing if it was stale.
6. Platform texting (unrelated to Gesheft but live NOW): pick the new from-number, carry the env in a commit, redeploy api + worker, re-probe A.
7. After FOC: verify `phone_numbers?filter[phone_number]=+18452449666` active; `TenantSmsNumber` TELNYX; `TextingRegistration.numberAssignments`; CNAM at Telnyx (VoIP.ms had cnam=1 — set the outbound CNAM name at Telnyx); human call + text both ways.

## 5. The draft and what is still missing
Telnyx porting order **`64106b7c-5ac9-4539-aa44-793c368c853b`**, created 16:45Z from inside `app-api-1` with the app's own credentials (`tmpx/portdraft.ts`, ephemeral). PATCHed: customer_reference `gesheft-8452449666`, misc.type full, end_user.admin {entity_name ezify, auth_person_name Israel Weinstock, billing_phone +18452449666, account_number 344022, pin_passcode 9593}, location 13 Kosnitz Dr / Los Angeles / NY / 10950 (verbatim VoIP.ms invoice, as on 723-1213), phone_number_configuration {connection 3049417907428656858, messaging_profile 4001a0ab-3699-450e-83b9-77ac48c3f0dc}. `requirements_met:false`. Allowed FOC windows: Mon–Fri 11:00Z–01:00Z starting **2026-09-22**.
Still to attach before `actions/confirm`: LOA PDF (the wizard's generator in `telnyxPortFiling.ts` — needs the typed signature Izzy supplies), the VoIP.ms invoice PDF (`getInvoice {from,to}` → invoice.php URL → upload as document → `documents.invoice`), `emergency_address_id`, `requirements` filled with both document ids, then FOC = Izzy's pick, then confirm. ⛔ Drafts are free; DELETE 204 if abandoned.

## 6. ⏳ NOT PROVEN / open
Real inbound call on a ported number; MMS to a handset on a major carrier (all probes ended on VoIP.ms); the switcher/engine timing on Gesheft's own row; CNAM after landing; the 662-3885 forward; the platform-sender outage (Izzy decides); WhatsApp door raw-body.

## 7. 18:06Z — the carrier test (Izzy: "proven by a phone carrier, not VoIP.ms")
From +18457231213 (Telnyx, NO campaign) to +15622096644 (T-Mobile, Wireless): SMS `4031a0b5-b2af-…` → `delivery_failed`, error 40010 Not 10DLC registered (DLR reached our door as `telnyx.sms_status delivery_failed` — the door works). MMS `4031a0b5-b2b4-…` with `media_urls=[signed m4a]` → `type MMS`, `to[].status sent`, media `audio/mp4 9132`, `completed_at null` after 2 min (T-Mobile MMS gives no final DLR). ⛔ Probe D's "unregistered sends are accepted" is TRUE ONLY toward VoIP.ms/Bandwidth destinations; toward wireless carriers the campaign assignment is mandatory. Plan §4 item 3 therefore = a hard ~2 h outbound-to-cell block after FOC for Gesheft.

## 8. SUBMITTED (2026-09-18 ~18:55Z)
Scripts `tmpx/port1.ts` + `tmpx/port2.ts` inside `app-api-1` (ephemeral), using the app's own helpers: VoIP.ms `getInvoice` → `/tmp/gesheft-voipms-invoice.pdf` (46,366 B, pdf-parse confirmed `8452449666` on it) → `uploadDocument` → `15d90305-eabf-4d47-ac1f-fe44dcfef969`; `buildLoaPdf` with signer "izzy wein" → `2ff7605a-5297-4ffd-bd24-0cf92e659783` (first upload `b76639c9-…` signed "Israel Weinstock" superseded); `createAddress` 33 Dinev Rd Kiryas Joel → `3051869631594628476`; `mapPortRequirements` → both ids; PATCH with `activation_settings.foc_datetime_requested = allowed_foc_windows[0] = 2026-09-22T11:00:00Z`; PATCH with `emergency_address_id` → 422 Invalid (ToS), re-PATCHed without; `POST actions/confirm` → 200 `in-process`. Post-confirm read: `requirements_met:false`, `phone_numbers: []` (both identical to 723-1213 right after its confirm).
**At FOC checklist:** Telnyx `phone_numbers?filter[phone_number]=+18452449666` active → switcher flips `TenantSmsNumber` (≤10 min) → engine attaches to C5GLU0S (`numberAssignments`) → `TextingRegistration.status live` once ASSIGNED; set CNAM at Telnyx; after Izzy accepts the Emergency ToS: `POST /phone_numbers/{id}/actions/enable_emergency {emergency_address_id: 3051869631594628476}`; ask a human to call + text 845-244-9666.
