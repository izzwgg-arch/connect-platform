# Loopcom Community — BUILT (2026-09-18): own api + web + mobile, own database, Loopcom SSO; NOT deployed (separate server later)

Full handoff: `docs/ai-context/AGENT_HANDOFF_LOOPCOM_COMMUNITY_BUILD_2026-09-18.md` (traps + state) — and the earlier design handoff `AGENT_HANDOFF_LOOPCOM_COMMUNITY_2026-09-18.md` (audit, mockups). Product docs live in `docs/community/` (README = how to run, CONVENTIONS, ARCHITECTURE, API/ERD/EVENTS/PERMISSIONS/DOMAINS generated from source, THREAT_MODEL, RUNBOOKS, parity, controls.json, COMPLETION_REPORT).

- Izzy: *"Build it like this … 120%"* then *"Do not deploy it … separate server with an API into Loopcom … people who log in through Loopcom won't need a separate login … dev server locally"* and *"kick off a whole bunch of Sonnet agents"*. Built as `apps/community-api` (Fastify 5 + Prisma 6, db `loopcom_community`), `apps/community-web` (Next 15.5, Connect tokens light+dark), `apps/community-mobile` (Expo 54). ⛔⛔ NOTHING deployed, nothing on the Connect server; `infra/community/docker-compose.yml` is the future box.
- ⛔ The ONE Loopcom integration: `apps/community-api/src/auth/loopcomSso.ts` verifies a Loopcom JWT against `LOOPCOM_API_URL/me`. The portal-side "Community" sidebar link (opens `/sso/loopcom?token=`) is a LATER portal change (Fourth Rule applies there).
- Proof: 237 api integration tests on the real db (`pnpm --filter @loopcom/community-api test`), Tier-1 loop 17 flows × 20 = 340/340 over HTTP, chaos 5/5, isolation 5/5, mobile 65/65 + android bundle export; load test found and fixed a 58 s feed (batched eligibility, index-backed trigram). **Playwright E2E on the final production build: desktop 47+1 skipped · iPhone/WebKit 48/48 · Android/Pixel 48/48, 0 failed** — the phone runs found 9 real defects the desktop run missed (Safari hydration wiping typed form text, 404 legal pages, dual-stack api, and a phone-width overflow class: every stacking grid needed `minmax(0, 1fr)`). 10-minute soak sample at 100 VUs recorded in the completion report.
- ⛔ Traps: this Bash tool cannot pass an apostrophe inside a quoted heredoc (use Write); Claude-in-Chrome cannot reach localhost — proof is Playwright screenshots (`e2e/tools/shot.mjs`); Next dev needs `unsafe-eval` in CSP (dev only); never `prisma migrate dev` here (tsvector drift → it offers a RESET); tests need `COMMUNITY_TEST_HOOKS=1 COMMUNITY_RATE_LIMIT_OFF=1`; the Prisma engine dll is locked while the api runs (stop it before `prisma generate`).
- ⛔ Phone-width trap: an implicit/`1fr` grid column is sized by its widest child, so a wide table inside an overflow wrapper still widens the page — every stacking grid is `minmax(0, 1fr)` in one `globals.css` rule; a NEW `display: grid` stack must join it. WebKit reports the test's own cancelled fetch as a `pageerror` too. E2E = one project at a time, workers=1, against `next start`.
- ⏳ Open: Next upgraded to 15.5.25 (done); Turnstile + ClamAV on the real server; passkeys on mobile need a native module; nothing run on a physical phone; SMTP/Google/Apple/Anthropic keys unset locally (mailbox mode, rules-mode concierge).
