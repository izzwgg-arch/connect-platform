# Face-to-face AI Support — mockup and architecture recommendation (2026-09-15)

Full handoff: **`docs/ai-context/AGENT_HANDOFF_FACE_TO_FACE_AI_SUPPORT_2026-09-15.md`**.

- ✅ **Scope corrected by owner after the earlier concept review:** retain the existing `FloatingAssistant` exactly as it appears now. Add one option only: **“Talk to Laybel”**. Laybel is the name for the existing Loopcom Assistant in its voice mode—not a new assistant, avatar, video experience, admin studio, support product, context store, tool registry, provider, or media-platform project.
- ✅ **Current review mockup:** `C:/Users/izzyw/.codex/visualizations/2026/09/15/01a0a601-3008-7cd3-af27-85b8c7495b42/ai-support-review/talk-to-laybel.html`. It mirrors the existing Assistant opening panel and inserts the single `Talk to Laybel` row above the established suggestions. Selecting it shows only an in-panel voice state to explain the interaction; no visual body is introduced.
- 🔄 **Live avatar scope reopened by owner (2026-09-15):** Laybel must be a real face-to-face session whose selected portrait comes alive in the call, retains the existing Loopcom Assistant as its intelligence layer, and is proven live end to end. The owner asked to review the existing portrait mockups before a provider is selected or any real call is activated. The existing LiveKit Meetings transport remains the preferred starting point; SignalWire is not assumed to be the video/avatar runtime.
- ✅ **Portal deployed (2026-09-15):** Deploy Center dry run `ad871db3…` passed for `portal` / `feat/ivr-migration-takeover`, resolving `7d93d23a8` (which contains the Laybel commits). Real blue/green job `c2ba7701…` completed **SUCCESS** at 6:16:54 PM after 5m23s, with stage `done` and deployed commit `7d93d23a8`. The build completed Next compilation, type validation, and static generation; public `/ready` returned `200 {"ok":true}` during the rollout. The mandated direct running-container source grep remains not independently proven because the canonical Linux SSH path was unavailable and the recovered Deploy Center UI would not open the log control; do not call that particular verification complete.
- ✅ **Interaction clarified (2026-09-15):** Clicking `Talk to Laybel` intentionally does **not** launch a video call or SignalWire call. It activates an in-panel push-to-talk state; the user then presses the existing microphone button, grants browser microphone access, speaks, and presses it again to submit. The clip goes to the established authenticated `transcribe` endpoint, its text uses the existing Assistant chat path, and a reply is read through browser-local speech synthesis when available. A real-time/video/SignalWire media session was neither requested in the corrected scope nor implemented.
- ✅ No avatar, video, LiveKit, WebSocket, new AI provider, media backend, database migration, policy change, PBX operation, or new support surface was added. Users explicitly start capture, can end it at once, and retain the existing typed composer.
