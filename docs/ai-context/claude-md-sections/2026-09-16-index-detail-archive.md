# CLAUDE handoff-index detail archive — 2026-09-16

Maintenance outcome and verification: `docs/ai-context/AGENT_HANDOFF_FACE_TO_FACE_AI_SUPPORT_2026-09-15.md`, current repair section. Standing rules and the original area links are unchanged.

Verbatim oversized index entries preserved when shortening the index to its standing size budget. Historical routing notes, not new verification: open the linked area summary and full handoff before work.

- 2026-09-16 · OCTOBER 2026 CHARGE PROJECTION + NEXUS REALTY CUT OVER OFF SOLA: **$1,901.04** across 18 autopay tenants (realistically **$1,846.04** — Secro's mis-mapped Sola link still blocks Oct 6); ~$2,153 counting Yossis $206.96 manual + Fixup $45 on Sola. Amounts are the ENGINE's own `/admin/billing/platform/tenants/:id/invoice-preview?periodMonth=10` run per tenant inside `app-api-1` (⛔ api listens on 3001, `jsonwebtoken` is not installed — hand-roll the HS256 token), not hand math. ✅ **Nexus: Sola schedule DISABLED (live-verified), Connect autopay ON, day 28→26, flat $65, first charge Sep 26** — ⛔⛔ the cutover helper wanted Oct 26 because it assumes Sola already charged, and a dormant $60 billing profile would have armed itself ([[sola-cutover-before-its-next-run-skips-a-month]]). ⛔⛔ ALSO FOUND: inii mini + Matamim + TYH lost ALL of September to a 17-hour paid-period overlap (`billing.autopay_skipped_period_already_paid`, ~$128, never caught up) and RSBK auto-charges $13.91 on Oct 20 with 4 ACTIVE billable extensions because `billingQuantityOverrides.extensions` is pinned manual 0 → `2026-09-16-october-2026-charge-projection.md`

- 2026-09-16 · THE EXTENSION ROAD IS THE MIRROR NOW, HARDENED (api `c892d4b9` deployed+verified): ⛔⛔ the code knew ONE licence sentence, so `vitxi_clients.max_reached` sailed past it and no fallback ran on a live customer. `apps/api/src/pbx/licenceRefusal.ts` = all 16 strings **with i18n keys, read off the PBX's en_US** (⛔ zh_CN words them differently), plus the two `import_extensions.*` **SILENT DOWNGRADES** — importer says "Completed Successfully" while clearing the WebRTC flag (desk phone where a softphone was sold, no other signal). Console create = **MIRROR FIRST**, panel fallback (`PBX_EXTENSION_CREATE_MODE=panel` reverts); ⛔ rows-landed+apply-failed never falls back; ⛔ a helper 200 is not proof; ⛔ under-cap no-op stays loud. 6 of 16 tests replay-FAIL on pre-fix HEAD. ⛔⛔ The deploy said `skip=no_changes` + `success` and shipped **NOTHING** (no state dir AND `/app/.build-commit` 1 byte → compared the commit to itself); fixed in deploy-api.sh **and** deploy-portal.sh, guard 2 replay-FAIL. ⏳ Izzy's Run button: `scripts/pbx/mirror/mirror-extension-grants-20260916.sql` — until it runs EVERY new extension is desk-phone-only (incl. Felix 103); no `/mirror/device-add`; onboarding still panel-road. Relax Tires 102+103 live, invites SENT, Sep 26 invoice $45→**$105** re-sent → `2026-09-16-relax-tires-two-new-extensions.md`

- 2026-09-15 · COWORKER WORKSPACE BUILT + DEPLOYED (`7b26c388`→`90e8a5ca`→`04fb30b9`): IDE-style chat, live plain-English steps, asks questions back, Stop, voice, any file read, folders + git projects, full page `/coworker` (key `can_view_workspace_coworker`, no default bucket). ⛔⛔ FOUR defects the green suite + typechecks + container greps ALL missed were found by typing ONE sentence into the live page — read them before touching this area: the last two were a card-era sentence surviving in a **tool description** (a model reads it whether or not it calls the tool) and the **full page never reaching the computer** while its pill said "connected". ⛔ the chat page is HOSTED, never the boundary — the desktop's policy core + NATIVE approval window decide. ⏳ still no real task run with the hands; rc.17 built, not installed → `2026-09-15-coworker-workspace-built.md`

- 2026-09-15 · ANDROID TABLET / EVERY-DEVICE COMPATIBILITY — ✅✅ **vc103 IS LIVE** (listing: "Updated on Sep 15, 2026" + the tablet note). Play filters on IMPLIED features, not `<supports-screens>`: six were required (telephony/camera/mic/bluetooth/location/screen.portrait); ⛔ `CALL_PHONE`→telephony is injected by **react-native-callkeep**, MERGED manifest only; `camera`=REAR. ✅ FIX = 16 `<uses-feature required="false">` in the bare manifest + the same list in a `withOptionalHardwareFeatures` plugin (prebuild would re-exclude); guard `deviceCompatibility.test.ts`. ⛔⛔ **"people search LoopCom and it doesn't come up" = TWO causes:** tablets were **absent from search entirely** (the Play app filters results by compatibility) — fixed today; phones are a **NAME COLLISION, not a bug** (`loopcom`→#1 but `loopcom app`/`loopcomm`→#2 behind "LoopCom Messenger", `loop com` with a space → not in the top 30; 1+ downloads = no ranking signal). Title is the lever — NOT changed, Izzy's call. ⛔ **Console is WALLED behind a new ToS acceptance on all 3 accounts — Accept deliberately NOT clicked**, so country availability / device-catalog count / rollout state are UNREADABLE. ⛔ uploads are manual (page-JS trick DEAD, file_upload 10 MB, browsers read-only, eas submit unconfigured); ⏳ no real tablet yet; ⛔ x86 still excluded → `2026-09-15-android-tablet-compatibility.md`

- 2026-09-15 · ONBOARDING WELCOME EMAIL → GOOGLE PLAY BADGE: ✅ BUILT + DEPLOYED `66eb7096` (api+portal, 0 restarts) — the outlined "Download Loopcom for Android" button is Google's official badge 180×70 linked to `play.google.com/store/apps/details?id=com.connectcommunications.mobile`; APK wording + sideload note deleted, plain text follows. ⛔⛔ THE REAL FIX: the block is UNCONDITIONAL and the template resolves BOTH URLs itself (`googlePlayListingUrl()` / `googlePlayBadgeUrl()`) — `androidApkUrl` is GONE from the input and `getAndroidApkUrlForInviteEmail()` is DELETED, so the 2026-08-09 "one caller passed null" class cannot recur (guard test). ⛔ badge width/height must stay ATTRIBUTES (Outlook draws 646px otherwise); asset's transparent margin IS Google's required clear space — never crop. `ANDROID_PLAY_STORE_URL` overrides, defaults to the listing. APK route still 200 — not withdrawn. PROVEN: deployed container builds the badge email, badge 200/4904b on both hostnames, 1414/1423 api tests (9 failures pre-existing elsewhere). ⏳ NOT PROVEN: no invite SENT since, Outlook never rendered it — close it by inviting a spare address and checking BOTH paths' EmailJob bodies. Mockup artifact YSi8QdS7P9NDFBUZJfEjWH → `2026-09-15-onboarding-welcome-email-google-play-badge.md`

- 2026-09-16 · LOOPCOM MOBILE FULL PRODUCT BUILT (mockups APPROVED → production, `5232cba2`): own sidebar section w/ 10 customer pages `/mobile/*` (one key per page, NO default bucket — granting IS the launch; Dashboard keeps can_view_workspace_mobile), 13-view console, separate LM- invoice ledger (idempotent by tenant+periodStart, never touches invoiceEngine), 8 emails on the EmailJob lane incl. once-per-tenant welcome (eSIM code NEVER emailed), additive migration `20260916030000`; suites green (4 portal + 1 api failures are PRE-EXISTING elsewhere); ⏳ zero real lines yet, launch = granting the keys; ⛔ NEXT ROUND = MOCKUPS ONLY awaiting Izzy: custom/private plans, buy eSIMs/SIM cards, one-page create-a-line (artifact EpAZXqGsGaQVPnhfk5XvXC); 09-15 eve GAP PASS (Izzy re-sent old brief): live = all empty states + owner side is one chip-tab page, 9 console settings saved-but-never-enforced, invoice has CSS "L" + window.print, no pay; ⚠️ Telnyx balance $26.32 < $50 floor; gap mockups `docs/mockups/loopcom-mobile/gap-pass.html` (artifact 1i8SFgTZ98WXdLe4NXrf9n) awaiting Izzy, nothing built → `2026-09-16-loopcom-mobile-full-product-built.md`

- 2026-09-15 · Desk Phone Wizard device identification + maker clouds; ⛔ NEVER asks a phone password — serial via typing/PHOTO/TEXT through ONE gate `recordLabel`; ✅ the FIRST real photo (Izzy's T42S, upside-down) beat all four OCR passes → **BARCODES ARE READ FIRST now** (zxing, checksum-backed, `4011fa5f`), OCR is the fallback; provision reuses the vouched serial ON FILE; Yealink managed models 7→37 (his T42S had no row); ⛔ his T42S MAC is RPS-claimed by ANOTHER org (800004; LAN PnP still wins at factory boot); ⛔ GDMS add is a BATCH (`3040f2bc`); ⛔⛔ wizard OWNS a phone on its own network (`7e427c28`); ✅ ROUND 18 — the CUSTOMER SCANS THEIR OWN PHONES: public-by-token `/phone-setup/<token>`, camera frames decoded SERVER-side through the same one gate, matched BY MAC and refused by maker name, four approved vendors only, one live revocable link per run → `2026-09-14-desk-phone-wizard-automatic-device-identification.md`
