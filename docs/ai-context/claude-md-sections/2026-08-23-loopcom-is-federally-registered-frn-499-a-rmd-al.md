# ⛔ AGENT HANDOFF — LoopCom is FEDERALLY REGISTERED (FRN + 499-A + RMD all filed) and the COMPLIANCE CALENDAR keeps it that way (2026-08-23) — READ FIRST before any FCC/USAC/RMD question, before touching `complianceCalendar.ts`, or before adding ANY regulatory deadline reminder

> Moved verbatim from `CLAUDE.md` on 2026-09-14 when CLAUDE.md was cut down to rules + index. Keep editing THIS file for this area; its one-line entry lives in CLAUDE.md's HANDOFF INDEX.


(Memory: [[loopcom-fcc-frn-and-federal-registrations]] + [[compliance-calendar-built]] — those two files are the authority; this is the pointer. Build commit `f0f5547c` on `feat/ivr-migration-takeover`.)

- ✅✅ **All three federal registrations are DONE as of 2026-08-23**: FRN **0038803722**; USAC 499-A **submitted** (Filer ID arrives by email, a Monday scheduled task watches); **RMD FILED and live in the database** — Option 3 "No STIR/SHAKEN Implementation – Performing Robocall Mitigation", exemption 47 C.F.R. § 64.6304(b), plan PDF v2 attached (`docs/regulatory/LoopCom_Robocall_Mitigation_Plan.pdf`, `64a62651`: customers CANNOT touch caller ID at all; 10DLC required for A2P texting). Izzy signed the perjury declaration himself. ⛔ **Never certify complete/partial STIR/SHAKEN** ($10k/violation for inaccurate filings); updates within 10 business days of any change; annual recert March 1.
- ⛔ **ServiceNow (RMD portal) form trap, proven the hard way**: JS `$setViewValue`/DOM-select writes are display-only AND select2-dropdown clicks can fail to reach the Angular model — **the sanctioned setter is `g_form.setValue()`** (walk scopes up to `scope.getGlideForm()`, wrap in `scope.$apply`, verify with `getDisplayValue`). Text fields need real typed keystrokes. Probe validation by clicking Submit while the perjury box is unchecked — it refuses and lists what is missing. And the 3-day “Okta page timed out” mystery was **the wrong of two connected Chrome browsers** — `list_connected_browsers` first.
- ✅✅ **THE COMPLIANCE CALENDAR IS BUILT** (`apps/api/src/complianceCalendar.ts` + portal `/admin/compliance` + a dashboard banner): seeded with RMD recert + CPNI (2027-03-01), 499-A + CVAA (2027-04-01), BDC (2026-09-01 + 2027-03-01), D.C. agent renewal (2027-08-20). Reminders: **SMS to 562-209-6644 + email to izzy@loopcom.net at T-30 days then weekly until marked done** (Izzy’s cadence, verbatim). ⛔ **Completing a yearly item ROLLS its dueDate forward one year** — the row never closes; seeded items refuse DELETE (the boot seed would resurrect them). ⛔ Delivery is deliberately **direct** (`resolveBillingSmsSender` + EmailJob type `COMPLIANCE_REMINDER`, never ADMIN_ALERT) — an AgentEscalation’s open-row dedupe would make weekly repeats one-shot. Sweep is hourly with a 3-min boot kick (`COMPLIANCE_REMINDERS_ARMED` boot line; kill switch `COMPLIANCE_REMINDERS_DISABLED=1`); cadence state lives on the ROW.
- ⛔ **The BDC 2026-09-01 seed fires a reminder immediately on deploy** — that is deliberate (it may genuinely be due in days); its details say to confirm whether a filer this new owes the 2026 round, and “Done for this year” rolls it to 2027 if not.
- ⏳ **Open**: 499 Filer ID email; NY state tax registration + the billing itemization (taxes on for B Visible/Create A Box/Displaydex with NO total change — Izzy’s standing constraint); 10DLC registration of existing texters when the SignalWire migration lands.
