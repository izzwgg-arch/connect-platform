# 2026-09-18 · GESHEFT 845-244-9666 → TELNYX, ZERO-DOWNTIME PORT — PREPARED, DRAFT AT TELNYX, NOT CONFIRMED (awaiting Izzy's signature + FOC pick)

Full handoff: **`docs/ai-context/AGENT_HANDOFF_GESHEFT_PORT_2026-09-18.md`**

Izzy: *"submit a port for their phone number 2449666 … zero downtime for phone and zero downtime for SMS … test the SMS from the end … be extra careful."*

- ✅ **THE REHEARSAL IS TODAY'S 723-1213 PORT (real timing):** submitted Wed 09-16 evening, FastPort FOC Fri 09-18 07:00 ET, **ported 07:04 ET**; texting row flipped VOIPMS→TELNYX by the switcher **89 s later** (11:05:53Z). Telnyx FOC windows are BUSINESS DAYS 7 AM–9 PM ET only (a draft for 2449666 lists Tue 09-22 as the earliest; no Sat/Sun).
- ⛔⛔ **FOUND + FIXED (api `9a27c130`, container-verified): BOTH TELNYX WEBHOOK DOORS REFUSED EVERY REAL DELIVERY AS 401 "unsigned"** since they were built. `fastify-raw-body` (`global:false`) hands out `req.rawBody` via an `onRoute` hook that only sees routes registered AFTER the plugin loads at `ready()` — every `app.post()` in server.ts runs before that, so no route ever had a raw body; the door tests faked `req.rawBody`. 16 refusals audited 09-16→09-18. Fix = per-route `preParsing` capture (`telnyx/rawBodyCapture.ts`) on `/webhooks/telnyx/sms` + `/webhooks/telnyx/mobile`; new real-Fastify test (3/4 fail on HEAD). ⛔ `/webhooks/whatsapp/meta` uses the same plugin pattern and is presumably equally dead — NOT touched.
- ✅ **LIVE-PROVEN AFTER THE FIX, all on Loopcom's own numbers (557-7768 VoIP.ms ↔ 723-1213 Telnyx):** inbound SMS to a Telnyx number → chat in 2 s (`telnyx:4286d208…`, first ever); inbound MMS image → chat IMAGE + stored attachment (image/png); outbound text from the Telnyx number through the REAL chat door (`/internal/chat/sms-system-reply` → worker → `TelnyxSmsProvider`) → delivered + DLR webhook → arrived on VoIP.ms in 6 s **with NO 10DLC campaign on the sender**; **voice note (m4a, audio/mp4) as Telnyx MMS → arrived byte-identical (9,132 B) in 10 s — NO MP4 workaround needed for Telnyx** (mobile records .m4a, portal composer prefers audio/mp4). Mobile needs nothing: provider is server-side; both clients infer images/audio from MIME.
- ⛔ Cosmetic drift, not fixed: the api-side MMS mirror (`connectChatRoutes.ts` `mirrorVoipMsInboundMmsRows`) omits `mediaKind` (defaults `file`) while the worker poll path sets it from MIME; both clients render by MIME so nothing visible.
- ⛔⛔ **PLATFORM TEXTING IS BROKEN SINCE 07:04 ET:** `billingSmsSender.ts` sends from 723-1213 via VoIP.ms → `invalid_did` (proven). Every pay link, receipt and SIGN-IN CODE fails silently. Fix options (Izzy's call): env `BILLING_SMS_FROM_NUMBER` → 845-305-0012 or 845-557-7768 (both Connect-owned, VoIP.ms, sms=1; needs a carrying commit + api AND worker redeploy) or make the sender provider-aware + file Loopcom LLC's own 10DLC.
- **Gesheft facts:** VoIP.ms routing `account:344022_gesheft`, **port_out_pin `9593`** (must be on the LOA/order), E911 registered at VoIP.ms "Gesheft, 33 Dinev Rd, Kiryas Joel NY 10950" (Telnyx validator: VALID as-is), VoIP.ms `sms_forward` → **845-662-3885** (stops at FOC; owner of that cell unknown), CNAM on. Traffic 60 d: 1,555 inbound texts + 27 images, **4 outbound**; no delivery-SMS, no SmsMessage sends. Main default-trunk has `_8452449666 → Gesheft` (same shape that carried 723-1213 → Loopcom today) so trunk-183 inbound voice needs no PBX change; outbound voice/CID unaffected (0001). Telnyx portability: portable, fast_portable, messaging_capable, Bandwidth CLEC NY.
- **The three lanes at FOC:** inbound voice = 0 gap (DID dispatch); inbound SMS/MMS = 0 gap (messaging profile rides on the order's `phone_number_configuration`, door fixed); outbound SMS = switcher ≤5 min → 10DLC sweep ≤10 min → Telnyx assignment ~2 h (⛔ Telnyx REFUSES pre-port attach: "+18452449666 was not found") — but unregistered Telnyx sends are NOT refused (proven), so exposure is carrier filtering on ~1 text/month. 911 = 0 gap ONLY if Izzy accepts Telnyx's Emergency ToS first (then the address rides on the order); otherwise Gesheft has NO 911 from FOC until accepted.
- ✅ **TELNYX DRAFT `64106b7c-5ac9-4539-aa44-793c368c853b`** (status draft, `requirements_met:false`, nothing filed): end_user admin ezify / Israel Weinstock / acct 344022 / PIN 9593, bill address as the VoIP.ms invoice (13 Kosnitz Dr, "Los Angeles" NY 10950), config = connection 3049417907428656858 + messaging profile 4001a0ab-3699-…; requirements = LOA + latest invoice. ⛔ Not attached yet: LOA (needs Izzy's TYPED SIGNATURE — never typed by the agent), VoIP.ms invoice (pull via undocumented `getInvoice`), `emergency_address_id`. ⏳ Awaiting Izzy: signature text, FOC window, Emergency ToS, the 662-3885 forward question, the platform-sender fix.
- ⛔⛔ **CORRECTION 18:06Z — an unregistered Telnyx number IS BLOCKED to real mobile carriers.** Plain text 723-1213 → Izzy's T-Mobile cell 562-209-6644: `delivery_failed`, Telnyx **40010 "The sending number is not 10DLC-registered but is required to be by the carrier."** The earlier 6-s delivery only worked because the destination was VoIP.ms/Bandwidth (no 10DLC enforcement). So Gesheft's post-FOC outbound gap (until Telnyx ASSIGNED, ~2 h) is a hard block to cells, not "possible filtering". **The m4a voice note as MMS to the same T-Mobile cell was accepted and reads `sent` (MMS is not 10DLC-gated the same way; T-Mobile returns no final MMS receipt) — ⏳ proof = Izzy's ear on 562-209-6644 (msg `4031a0b5-b2b4-4a53-8a66-db848dd3a166`).**
