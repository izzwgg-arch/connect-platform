# Browser Companion — local implementation, not release-ready

Full handoff: `docs/ai-context/AGENT_HANDOFF_BROWSER_COMPANION_2026-09-14.md`.

- Actual installed Loopcom access recovered and the logged-in dashboard was observed on resume. Its Google sign-in window title was stale. Source browser was hidden Electron, not real Chrome.
- Added MV3 companion, authenticated loopback bridge, nine normalized Chrome tools, local approvals/audit/artifacts, conversation-scoped tabs, controlled acceptance site and tests. No installation/publication/deploy.
- Latest: Coworker 43/43, extension security 3/3, isolated installed-Chrome components 10/10, TS6 typecheck/build pass. Added generated shared schema, one-use approvals bound to task/document/target/arguments, cancellation checkpoints, command deduplication, CSS-hidden redaction and bounded page output. Clean NSIS candidate built in scratchpad; packaged files and all 7 embedded icon frames verified. Upload verifier now checks actual file bytes/hash; its binary multipart + origin rejection test passes.
- Real app → model → extension → real Chrome, both providers, vision, restart/recovery, stress/security matrix and packaged acceptance remain unproven. Several features still need implementation; full handoff distinguishes them.
- Owner explicitly authorized full desktop action. On the second resume, supported address-bar navigation was attempted; the Windows tool again stopped on opening chrome://extensions because it could not verify the URL for policy. The extension remains uninstalled, but the later Playwright candidate was installed. This is not yet live-chat/provider acceptance or production readiness.

- **2026-09-15 Playwright implementation:** Loopcom now uses Playwright Core to launch installed Chrome lazily in an isolated `Loopcom Coworker` profile, replacing the extension bridge as the active browser engine. The Settings & Connections page is Loopcom-branded and explains the separate profile. The model still receives only the nine normalized `computer_chrome_*` commands; it cannot enumerate ordinary Chrome tabs or execute arbitrary JavaScript. Writes require a one-use local approval bound to the command, task/conversation, tab, URL, epoch, target and a private form-state digest. Local integration test passed tab isolation, approval replay rejection, page-change rejection, form fill, and same-origin download. Production `playwright-core@1.63.0` is explicit and included in the NSIS payload.
- The candidate `scratchpad/browser-companion-package-20260915162157175/release/Connect-Setup-0.1.17-rc.16.exe` (SHA-256 `F015056D9E3D85B73C3328D816D7F662AA699C3C2440203FE79436318987C85B`) was installed with owner approval on 2026-09-15. Loopcom was restarted from `C:\Users\izzyw\AppData\Local\Programs\@connectdesktop\Loopcom.exe`; its installed ASAR contains the runtime, `playwright-core`, and branded settings page. The unpacked candidate executable passed all seven Loopcom icon-frame checks. Commit `3b414de0` is present on `origin/feat/ivr-migration-takeover`. Live Loopcom-chat/provider acceptance remains unproven.
- This resolves the local extension setup block for the Coworker browser route. It intentionally does **not** reuse everyday Chrome sessions; a future existing-session mode still needs a supported Chrome consent/setup path and separate security review. Full comparison and primary sources are in the handoff.

- **2026-09-15 Coworker IDE mockup:** The owner asked to review the chat redesign before any product work. The mockup shows one unchanged workspace structure in light and dark themes: task history and New task in the left rail; the user task, Coworker response, tool rows, approval, and composer in the center; active computer run and artifacts in the right rail. It intentionally exposes the actual work lifecycle without showing raw model/tool payloads. This is a visual review artifact only: no app UI, provider flow, browser policy, package, or deployment changed. Await owner feedback before implementation.
