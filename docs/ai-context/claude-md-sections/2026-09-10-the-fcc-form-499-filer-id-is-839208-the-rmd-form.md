# ⛔ AGENT HANDOFF — the FCC Form 499 Filer ID is **839208**, the RMD form has NO field for it, and the FCC's public 499 database is a stale USAC snapshot (2026-09-10) — READ FIRST for "the 499 Filer ID isn't in the Robocall Mitigation Database", before revising the RMD filing, or before quoting the Filer ID

> Moved verbatim from `CLAUDE.md` on 2026-09-14 when CLAUDE.md was cut down to rules + index. Keep editing THIS file for this area; its one-line entry lives in CLAUDE.md's HANDOFF INDEX.


(**Docs + one RMD revision staged in Izzy's Chrome for HIS signature — no code, no deploy, no server touched.** Memory: [[loopcom-fcc-frn-and-federal-registrations]].) Izzy, 2026-09-10: *"499 file ID is not under Robo mitigation. We need to update it … I tried to look it up yesterday, didn't find it."*

- ✅ **The Filer ID is `839208`** (USAC approved the 2026-08-23 499-A; the email went to izzy@loopcom.net, which is NOT the Gmail connected to Claude — Izzy supplied the number). Recorded in memory + `docs/regulatory/`.
- ⛔⛔ **THE RMD FORM HAS NO 499 FILER ID FIELD.** All 81 fields of the Revise form were dumped (`g_form.getFieldNames()`): FRN, names/address, filing type, principals, other FRNs, contacts, call-chain role, enforcement history, OCN, certification option + exemption text, PDF upload, ECFS flag, declaration, e-signature. **The only place the Filer ID lives in the RMD is the attached plan PDF**, which v2 stamped "Pending — USAC registration submitted August 23, 2026".
- ⛔ **"It's not coming up" = the FCC's public Form 499 Filer Database (`apps.fcc.gov/cgb/form499`) is a USAC snapshot "as of Aug. 21, 2026"** — two days BEFORE the 499-A was submitted. Filer ID 839208 and FRN 0038803722 both return "No records" there until the FCC refreshes the dump. Lag, not a missing registration; nothing to file.
- ✅ **Plan v3 built and staged:** `docs/regulatory/LoopCom_Robocall_Mitigation_Plan.{html,pdf}` = v2 text verbatim + Filer ID 839208 in the header table + a one-line revision footer (headless-Chrome render, PyMuPDF-verified, 3 pages). Uploaded through **Revise** on the live RMD record as `LoopCom_Robocall_Mitigation_Plan_v3_2026-09-10.pdf` — the single-attachment upload REPLACES the old PDF in place (no delete step). Every copied field verified intact: `no_stir_shaken=true`, exemption text present, confidential=false. ✅ **SUBMITTED by Izzy 2026-09-10** — "My Filings" lists `LoopCom_Robocall_Mitigation_Plan_v3_2026-09-10.pdf`; the RMD record is current. Nothing else is owed to the FCC for the Filer ID.
- ⛔ RMD sign-in = Okta + **Okta Verify push to Izzy's phone**; the "page has timed out" screen means sign in at `fcc-ext.okta.com` first, then reopen `/rmd`. ⛔ `javascript_tool` results containing sys_id-shaped hex are BLOCKED by the redactor — return labels only, replace `_` in names.
