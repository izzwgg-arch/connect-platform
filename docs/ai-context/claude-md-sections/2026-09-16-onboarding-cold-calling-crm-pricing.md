# Onboarding link: cold-calling ($65/ext) + CRM ($20/ext) pricing, and the phone-wizard step-1 dead end — 2026-09-16

Full handoff: **`docs/ai-context/AGENT_HANDOFF_ONBOARDING_ADDON_PRICING_2026-09-16.md`**

Izzy, 2026-09-16, while Swift Mechanics was signing up live: *"this customer is going to be $65 an extension since it's cold calling. Make sure, when the invoice is created before they pay, that it's $65 per extension plus anything else"* → *"before I send out the link … I click a box: this is a cold-calling company … [N] extensions cold calling and one regular"* → *"cold calling is always double … also add the CRM … $20 a month extra per extension for CRM."*

- ✅ **DEPLOYED `014c0399` (api + portal), container-verified** (`.build-commit` on both; `onboarding_addon` in the api; the phone fix + "Cold-calling company" strings in the shipped portal chunks; /admin/onboarding 200).
- **Setting** = `answers.pricing = { coldCalling?: {extensions: "all"|N}, crm?: {extensions: "all"|N} }`. ⛔ SERVER-OWNED: `serverOwnedAnswers.ts` always carries the stored value and DROPS a client-sent one (lazy-create path too). ⛔ Not `internalNotes` — the admin notes box overwrites that column with a string.
- **Set it**: `/admin/onboarding` → Full sign-up → tick "Cold-calling company" / "Add CRM", count blank = all. Existing link: `PUT /admin/onboarding/submissions/:id/pricing` (SUPER_ADMIN, refused after payment; no UI button yet).
- **One quote** (`packages/shared/src/onboardingPricing.ts`): cold-calling lines REPLACE the $30 line at $65; CRM adds $20 each; counts clamp to the extensions set up. ⛔ The review `/quote` route builds its own input — it re-resolves both add-ons against the live count (otherwise review shows $30 and checkout charges $65).
- **Month 2** (`applyOnboardingAddOnBilling`, runs at checkout AND after the build, NOT behind the fee-config guard): cold calling on all → `extensionPriceCents 6500`; on N → recurring `$35×N` difference line; CRM → recurring `$20×N` line (`billingRecurringCustomLines`, tagged `source:"onboarding_addon"`, replaced each run). ⛔ Partial/CRM counts freeze at build time — an extension added later gets no CRM/cold-calling line. ⛔ Billing only — CRM access itself is NOT turned on for the tenant.
- ✅ **UPDATE (later 09-16): Izzy dropped CRM for Swift Mechanics — both their sign-ups (`cmu4kauj…` abandoned at step 1, and the one they actually used, `cmu4no5ms0epepb12sk6bvztk` office@swiftmechanics.net, #(929) 551-1319) now carry cold calling ALL only; live quote 1 ext = **$70**. ⛔ The customer restarted on a SECOND link with no pricing and reached review before it was caught — pricing does NOT carry to a new link; tick the boxes on every link for a cold-calling customer.**
- ✅ **LIVE on Swift Mechanics** (`cmu4kauj00hufli1ktnu7zt17`, jordan@swiftmechanics.net): stamped cold calling all + CRM all (Izzy's pick); the live `/quote` on their link reads **1 ext $90, 2 = $175, 3 = $260** (= $65 + $20 per ext + $3 E911 + $2 fees).
- ✅ **Phone-wizard dead end FIXED**: step 0 done in the DESKTOP layout (company + name only), then the PHONE layout's "Where are your phones?" demanded "A valid phone number" with no field for it (Swift Mechanics hit it live; email + cell saved empty). Missing email/cell on phone step 1 now goes back to "About you" with the message. Guard in `apps/portal/lib/onboardingSignalWireWizard.test.ts`.
- ⏳ **NOT PROVEN**: an autosave AFTER the stamp keeping `pricing` on the live row (unit-tested only; watch armed); the real checkout invoice amount (no invoice yet at writing); month-2 lines on a real tenant; the admin checkboxes clicked in a browser; the phone fix on a real phone.
