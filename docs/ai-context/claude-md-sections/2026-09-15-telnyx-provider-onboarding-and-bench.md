# Telnyx: account staged to the Trial ceiling, SIP pair built, A-attestation rules cited, /apps/telnyx bench BUILT — 2026-09-15

Full handoff: **`docs/ai-context/AGENT_HANDOFF_TELNYX_ONBOARDING_2026-09-15.md`**

- ⛔ **The real account is `izzy@loopcom.net` (TRIAL level, $5 credit, 2FA on).** izzywgg@gmail.com is a bare Pretrial account signed into by mistake (one stray inert SIP connection left there); support@connectcomunications.com has a dormant account nobody pursued.
- **On the Telnyx side now**: credential SIP connection **Loopcom-Primary-SIP** (`3049417907428656858`, user `userizzy67753`, password stays in Telnyx, G711U-first + Opus, RFC2833, SHAKEN Identity header ON) assigned to outbound profile **Loopcom-Primary-Outbound** (`3049418915823224570`, US+CA only, $10/day spend cap, $0.30/min max rate). All free; Trial blocks number purchase + calls until Izzy's 5-minute Paid upgrade (address → card → phone verify).
- **Attestation (cited in the handoff)**: A = caller-ID number is ON the account, automatic, free, no vetting — purchased or ported. Verified-external → B; unowned → refused 403 D51. Signing keys off PPI>PAI>RPID>From, so the PBX must not leak a stray PAI. Per-call proof = the CDR `stir_shaken` field, which the bench's Call-records panel reads. Own cert NOT needed for A; Eighth-R&O triggers + $100/mo hosted-cert path documented for later. ⚠️ Support Issue **#666221**: the AI said ported→B, contradicting Telnyx's own docs (ported→A); pushback for a human answer was pending at session end.
- **Provider architecture researched**: Managed Accounts = the real reseller feature but approval + committed-use-plan gated (Izzy's call); flat account scales with customer_reference/tags/billing groups meanwhile; 10DLC = one brand per downstream business; CNAM free per number (per-customer names, never LOOPCOM); E911 per-DID with the customer's real address; porting has a FULL API incl. end-customer LOA — the gap SignalWire can't fill.
- ✅ **/apps/telnyx bench BUILT** mirroring the SignalWire bench contract: `apps/api/src/telnyx/` (credentials in AgentSecret `telnyx_credentials`, fetch client, requireOwner routes, `telnyx.*` audit), portal page, nav + shared-catalog key `can_view_apps_telnyx`, SUPER_ADMIN force line + Locked list. **14 tests pass, 5 wiring guards read 0 against HEAD**; api tsc 0 new (85 = ambient incl. another session's in-flight 24168 error); portal tsc 0; portal suite 632/636 (4 pre-existing failures in files this task never touched).
- ✅ Drive-by: portal package.json test glob had a missing space silently unregistering 2 test files; re-enabling them caught a native `<select>` in OrdersDesk → ConnectSelect. Both fixed.
- ✅ **DEPLOYED AND CONTAINER-VERIFIED 2026-09-15**: commit `514934f7` (in branch tip `b1f6357c` — the Yealink session's deploy shipped the api, my direct deploy shipped the portal; the global compose-build lock serialised them). `app-api-1` + `app-portal-1` both `.build-commit = b1f6357c`, **0 restarts each**, health **200 on both hostnames**; api container carries `/app/apps/api/src/telnyx/` + 2 `registerTelnyxRoutes` matches in server.ts; portal ships chunk `app/(platform)/apps/telnyx/page-a51624ecb542ef4d.js` (verified by STRING `tx-wrap`, never a function name).
- ✅ **SAME DAY, WAVE 2 (`c0e12018`, api + portal DEPLOYED and container-verified, 0 restarts, 200 both hostnames): the account is UPGRADED TO "VERIFIED" (Telnyx's top self-serve tier — email/checks/phone/address/2FA/payment/KYC/AI-eval ALL green, balance $10)** after Izzy's card cleared on the 3rd attempt (⛔ the two declines were the billing-address form; "1 attempt from account review" was reached — never brute-force that form). Service address on file = the 499 address (33 NY-17M, Suite C, Harriman NY 10926; Israel Weinstock; Tax ID left blank). **A number is BOUGHT with Izzy's approval: (845) 306-6825, $1 + $1/mo, attached to Loopcom-Primary-SIP in the cart itself** — "Number Orders completed successfully", status Purchase pending → active on its own.
- ✅ Wave 2 in the repo: **the WIZARD-CARRIER SWITCH** (`resolveOnboardingNumberProvider` — stored AgentSecret override > env > voipms, fail-open, publicRoutes' 3 sites converted; owner-only GET/PUT `/admin/carrier-switch`; card on /apps/telnyx; ⛔ telnyx refused with the reason until the wizard grows a Telnyx path — the honesty rule) + bench panels for **number lookup, messaging profiles, RCS (agents + send w/ SMS fallback), Email API (domains + test send), porting orders (list + DRAFT only — deliberately NO submit route), and the beta Meeting bot**. 26 tests; onboarding differential clean (25=25 pre-existing in pbxTenantBuild/setupOrchestrator, zero new).
- ✅✅ **PROVEN WITH A REAL ANSWERED CALL (same day, afternoon): PBX trunk 183 "Telnyx Loopcom-Primary" is REGISTERED (38ms), T102's route 123 + inbound route + DID all wired to it (backups on the PBX, /root/*telnyx-20260915T115327Z*), route CID moved to 8453066825 (⛔ unowned CID = Telnyx 403 D51, no send_as net), Telnyx's dnis_number_format=national made inbound the VoIP.ms shape (NO custom dialplan), and Izzy's cell ANSWERED an 18s two-way call from (845) 306-6825 — Telnyx's own CDRs read  on ALL THREE test legs.** ⛔ CDR record_type is  + field  ("voice" is a 400) — pinned in the bench client. Handoff §10.
- ⏳ NOT PROVEN: no API key saved into the bench yet (Izzy was pasting it), the number's first CALL and its `stir_shaken=A` CDR are still the acceptance test, RCS/Email/Meeting panels never exercised against the live account, support pushback on ported-DID attestation unanswered (Issue #666221), PBX trunk not built (Izzy's gate — recipe in handoff §7).

- ➡️ **2026-09-16: the sign-up WIZARD now runs on Telnyx** (switch=telnyx, real sign-up built end to end) — see `2026-09-16-telnyx-onboarding-wizard.md` and handoff §11–14. The "telnyx refused by the switch" bullet above is superseded.

- ➡️ **2026-09-17: "Level 2" IS the VERIFIED tier — nothing left to request.** Telnyx runs two naming schemes: legacy accounts show *Level 1 / Level 2* (Account Settings → Verifications, `portal.telnyx.com/#/account/my-account/verifications`, form → "Request Level 2 Verification", ≤48 h review, appeals to kyc.verifications@telnyx.com); accounts made after June 2025 (ours) show *Trial → Paid → Verified → Enterprise* (`#/account/account-levels`), and Telnyx's own 10DLC/ISV docs treat Verified as satisfying the "Level 2" requirement. izzy@loopcom.net has been VERIFIED since 09-15. Only Enterprise (sales-qualified, contact Telnyx) sits above it: number blocks, LRN migration, Managed Accounts API, DDoS APIs. ⛔ The portal was logged out in Chrome on 09-17, so this is docs + the 09-15/09-16 handoff state, not a fresh screen read.
- ➡️ **2026-09-17 (Izzy: "at what point do I need to upgrade / do I tell them I'm a carrier?"): VERIFIED IS THE CEILING OF SELF-SERVE — the next rung is a COMMIT PLAN, not a verification.** Above Verified there is only Enterprise (sales-qualified, $5k+/mo) and the commit plans (Starter $1k/mo min, Growth $2k/mo min, quarterly billing, 8 %/15 % off) — those are what unlock **Managed Accounts** (the formal reseller/sub-account feature, Telnyx must also approve you as a manager account; NOT on pay-as-you-go). Until spend is near $1k/mo the flat Verified account + customer_reference/tags/billing groups IS the reseller shape and is what Telnyx's own ISV docs describe. **What bites FIRST is the outbound channel cap: Verified = 10 concurrent outbound calls account-wide** (D1 `403 User channel limit exceeded`); raise it by emailing support@telnyx.com — no upgrade needed, they want a use-case writeup only past 100 channels. Our own `Loopcom-Primary-Outbound` profile also carries a self-set $10/day spend cap and per-profile channel limit — ours to raise in the portal. Messaging throughput is per 10DLC campaign (per customer brand), account-wide default 50 MPS; payment/top-up limits are already gone at Verified. **No "carrier" flag exists and no disclosure is required** — reselling voice you ORIGINATE to your own end customers is the normal use; ToS §11.19 forbids only relaying/transiting traffic ANOTHER carrier originated without a signed agreement. Obligations that flow down to Loopcom as a reseller: AUP on end users (§10.1), a correct E911 address per DID (§14.4), traceback responses (§11.13), STIR/SHAKEN compliance (§11.17 — 499 filer 839208 + RMD filed), one 10DLC brand per downstream customer. Practical advice given: say it anyway when asking for the channel raise ("interconnected VoIP provider, FCC 499 filer 839208, RMD-registered, N tenants, need X channels") — it is exactly the use case they ask for.
- ➡️ **2026-09-17 evening: THE CHANNEL RAISE IS REQUESTED — Telnyx ticket Issue #677879, filed from inside the signed-in portal.** Filed through the portal's chat-bubble ("Chat with us", a Pylon widget, top-bar speech-bubble icon) as Izzy asked, wording: interconnected VoIP provider, 499 filer 839208, RMD-registered, 60+ numbers migrating via Loopcom-Primary-SIP, human-dialed business voice only, attestation A — **raise the account-wide outbound channel limit from 10 to 200 or the highest grantable, plus the process for further raises**. Telnyx's AI (TAIA) acknowledged, queued it for the human team, and answered the inbound half: **inbound concurrent calls have NO default limit** — an inbound cap is a self-serve SIP-Connection setting, no ticket needed. ⏳ The GRANT is not in yet; when it lands, our own self-set caps still clip us: `Loopcom-Primary-Outbound` carries a **$10/day spend cap** and any profile/connection channel limit — Telnyx honours the lowest, so those need raising in the portal (Izzy's call, they are spend guards). ⛔ Browser notes: BOTH Chrome instances were logged out (his "open" Telnyx tab was a stale page); login = portal "Send me sign-in link" to izzy@loopcom.net which HE clicked; the Pylon chat wedges this machine's renderer on >300-char type bursts — type in ~150-char chunks with waits, and a 30s CDP timeout usually still lands its keys (verify by screenshot, don't re-type blind: the first "failed" chunk HAD landed and re-typing would have doubled it).
