# The sign-up wizard runs on TELNYX — switched live, end-to-end proven except a human call and a real port — 2026-09-16

Full handoff: **`docs/ai-context/AGENT_HANDOFF_TELNYX_ONBOARDING_2026-09-15.md` §11–§14** (appended to the Telnyx handoff — same area).

Izzy, 2026-09-16: *"switch the onboarding wizard to Telnyx. It's going to be Telnyx for now, and then stress test the fuck out of it because I want to have people use it today. Make sure that it works end to end. I'm even giving you permission to buy one phone number for the test, and then same with porting."*

- ✅ **LIVE: the wizard carrier switch reads `telnyx`** (stored AgentSecret `onboarding_number_provider_override`, audit row written). New sign-ups search + buy on Telnyx. Stamped drafts keep their carrier. `TELNYX_AUTO_PROVISION=on` in BOTH api compose blocks.
- **Built** (api only + one portal line): `telnyx/telnyxOnboardingClient.ts` (search/orders/E911/porting/10DLC/messaging — shapes read off the live account), `onboarding/telnyxNumbers.ts` (search: 429-safe throttle+retry+60s cache, Monsey→SPRING VALLEY alias, no statewide fallback), `onboarding/telnyxProvisioning.ts` (ONE order pre-assigned to Loopcom-Primary-SIP + "Loopcom Sign-ups" messaging profile; order id persisted before polling; adopt-if-owned; CNAM; E911 "provisioned" only when Telnyx says active), `onboarding/telnyxPortFiling.ts` (real Telnyx porting order: LOA PDF + bill uploaded, requirements mapped, PATCH, CONFIRM; refusals → Port queue `needs_attention`), `onboarding/telnyxPortWatchdog.ts` (10-min sweep: lands ported numbers — routing, 911, PBX destination, caller-ID switch, texting, email — and confirms pending 911), 10DLC chain made carrier-generic (`registryFor` in signalWireTenDlc.ts), PBX build on shared trunk **183 "Telnyx Loopcom-Primary"** + 0001 backup.
- ⛔⛔ **FIVE LIVE BUGS the real run found that 42 green unit tests could not** (all fixed + deployed, several PRE-EXISTING and carrier-independent):
  1. **Autosave wiped the carrier stamp** (answers replaced wholesale) → at payment the number stage would default to VoIP.ms. Fixed: `serverOwnedAnswers.ts` carries `phone.provider` + `provisioning`; submit pins the carrier. ⛔ `texting` is wizard-owned — never carry it. (Hit SignalWire too.)
  2. Telnyx refuses `messaging_profile_id` on the general number PATCH (422/10027) — it's `PATCH /phone_numbers/{id}/messaging`.
  3. **Shared-trunk builds had NO Main DID dispatch** — the number rang nowhere. A bare Main apply does NOT fix it (mirror queues nothing for Main module 99); **panel `saveTenant` with the tenant's own unchanged number list, THEN `applyAndRebake(Main)`** does (proven `_8457774807 → T143_default-trunk`).
  4. An interrupted build could never resume: tenant lookup trusted the 40-min-stale REST list → now reads `ombutel.ombu_tenants` first.
  5. Sync `pbx_tenant_not_in_directory` for a new tenant → DB fallback with the FULL tenant table (⛔ the directory sync DELETES unlisted rows — never pass one row).
  Plus: 429s past ~17 parallel searches, Monsey not a rate center, "COMPTON:COMPTON DA" localities, resume false-alarm on the emergency location, backspace bytes a heredoc put in a regex.
- ✅ **PROVEN LIVE**: 45-search parallel stress; the whole wizard in Chrome → Sola pay page with the right $35 quote; carrier stamp survives every autosave; **(845) 777-4807 BOUGHT** (one order; retry adopted it — no double purchase), routed to Loopcom-Primary-SIP; PBX tenant 143 "Loopcom Telnyx Test bqy1lb" on trunk 183, outbound route 180 CID 8457774807, Main dispatch present; billing moved to the live tenant; ext 101 synced; owner promoted; invite SENT; **status ACTIVE**. Telnyx webhook **public key fetched via `GET /v2/public_key` and saved** (`publicKeySet:true`) — Telnyx inbound SMS is no longer fail-closed-blocked. Port filing proven on live DRAFTS (every field + LOA/invoice docs + requirements land; drafts deleted 204, nothing submitted).
- ⏳ **NOT PROVEN**: (a) **no real call** to/from 845-777-4807 — Izzy must call it; (b) **no real port submitted** — needs a number Izzy picks + its account #/PIN + a recent bill; `requirements_met` stays false on a draft, only a real confirm settles it; (c) the card leg was NOT re-run — test invoice CC-202609-00012 was marked PAID in the DB (no money moved; unchanged code, proven 2026-08-04); (d) Telnyx 10DLC filing never run (needs a real EIN); (e) landing sweep never ran on a real port.
- ⛔ **Business facts**: Telnyx has **ZERO stock in 212, 718, 347, 646, 917, 332, 201** (checked live, best_effort too) — NYC-core customers get 929 or must port. 845/914/516/631/929/732/848/973/908/551/862 stocked. Telnyx **refuses some E911 addresses for "manual validation" (85009)** — incl. Loopcom's own 33 NY-17M Harriman; the build then says "911 needs a person" and withholds the E911 email (correct).
- 🧹 Test debris: tenant 143 + Connect tenant for "Loopcom Telnyx Test" (izzy+telnyx-e2e@loopcom.net, fake-PAID invoice CC-202609-00012) — **wipe after Izzy's test call**; test #1 submission CANCELED, invoice CC-202609-00011 VOID, its bare tenant left (voided invoice pins it). Two messaging profiles exist ("Loopcom Chat" from unified messaging, "Loopcom Sign-ups" from this) — same webhook, harmless.

- ✅ **Evening follow-up (handoff §15), DEPLOYED `826f181c` + `42cccd3a`, container-verified:** 911 addresses rewritten to the postal form + validated first (**live: `33 State Route 17M, Ste C` created at Telnyx** — Izzy's "two ways": State record/Facebook `NY 17M` vs postal `State Route 17M`); owner TEXTED via AgentEscalation when a finished sign-up has no 911; automatic retry (hourly ×6, then 6-hourly; fired live) + `POST /admin/onboarding/submissions/:id/retry-e911`; Telnyx ports request **FastPort** at the earliest allowed window when eligible. telnyxOnboarding **49/49**.
- ⛔⛔ **911 on ANY Telnyx number is blocked until Izzy accepts Telnyx's Emergency Terms of Service** in the portal (10015 on enable_emergency).
- ⏳ **Port of (845) 723-1213 prepared in the wizard, NOT filed:** Izzy chose "through the wizard anyway" after the blast radius (platform pay-link/receipt/sign-in texting stops at switch-over; lands in a new tenant). Waiting on Izzy: name on account, typed signature, VoIP.ms bill (<90 days), payment. FastPort-eligible; earliest window ~2 business days out.
- ✅ **PORT OF (845) 723-1213 SUBMITTED** (handoff §16): Telnyx order `58969290-fbda-4842-bd13-399472c1fe5f`, in-process, **FastPort requested Fri Sep 18 07:00 ET**, LOA + VoIP.ms invoice (pulled via the undocumented VoIP.ms `getInvoice` API) attached; bill details used verbatim (ezify, 13 Kosnitz Dr, "Los Angeles" NY 10950, acct 344022, no PIN). ⛔⛔ Guard shipped first (`e0e1ef28`, live): a ported number already on our PBX (723-1213 → T35) is never added to the new tenant — the PBX has no DID uniqueness and the build would have hijacked the main line; verified after the build: only T35 owns it. ⛔ At switch-over pay links/receipts/sign-in codes stop sending (VoIP.ms sender) unless moved first. Temp number (845) 460-9054, new "Loopcom LLC" tenant, invoice CC-202609-00013 marked paid in DB (no card).
