# LoopCom Mobile: branded mobile service (eSIM/SIM/data) on Telnyx wireless is BUILT END TO END and INERT until the key is granted / the first eSIM is bought — 2026-09-15

Full handoff: **`docs/ai-context/AGENT_HANDOFF_LOOPCOM_MOBILE_2026-09-15.md`**

- ✅ **Telnyx wireless is ENABLED on the real account (live-probed from app-api-1)**: sim_cards/groups/orders, eSIM purchase (`POST /actions/purchase/esims`, whitelabel SPN "LoopCom" supported), activation-code QR endpoint, usage detail records (`record_type=sim_card_usage`), data-usage notifications, `/mobile_phone_numbers` + `/mobile_voice_connections` all answer 200 (0 SIMs yet). ⛔ **Mobile Voice/VoLTE is BETA at Telnyx** (endpoints live, config docs "coming soon") — the product reports it "beta" and promises nothing. SMS on mobile DIDs needs a messaging profile + 10DLC (none). E911 = per-number, mobile rules differ, 933 tests.
- ✅ **Built**: 7 additive Prisma tables (migration `20260915180000_loopcom_mobile`); `apps/api/src/loopcomMobile/` (wireless client on the bench transport — no SDK, mutations never retried; capability prober; pure plan/recount math; lifecycle service with purchased-but-not-saved auto-recovery via `line:<id>` tags; sweeps every 15 min, zero-footprint fast path; tenant routes on **`/mobile-service`** + owner console on **`/admin/mobile-service`** — ⛔ `/mobile` and `/admin/mobile` are the PHONE APP's prefixes; Ed25519 fail-closed webhook door `/webhooks/telnyx/mobile`, dedupe = exactly-once). Portal: `/mobile` customer page (eSIM QR install, pause/resume/lost, plans, port drafts) + `/admin/mobile-console` (capabilities, plans w/ margin, fleet, confirm-gated eSIM purchase, diagnostics, webhook log). Toggles per the fourth rule: `workspace.mobile` (NO force line, NO default bucket — granting the key IS the launch) + `admin.mobile_console` (SUPER_ADMIN-forced + Locked).
- ✅ Tests: 17 api (money math incl. twice-run recount identity, one-request purchase, real Ed25519 tamper/stale/wrong-key refusals, source guards) + 5 portal nav/page guards; permissionToggleCoverage 37/37 with the new rows; portal tsc 0; api tsc 87 = 85 ambient + 2 of the same ambient Timeout class. ⛔ Guard lesson: stripComments EATS server.ts regions (a `/*` inside a string) — match server.ts raw.
- ⛔ **Nothing bought, nothing filed, nothing billed**: no eSIM/SIM purchased, no port submitted (drafts only, no submit route exists), webhook publicKey not yet saved (probe: publicKeySet:false) and no URL registered at Telnyx, and **live-invoice wiring deliberately NOT flipped** — `buildMobileBillingLineItems` is tested + previewed on the console, but touching `invoiceEngine` is the money path and happens with the first real line as its own traced change (billingReconcile recount rule).
- ⏳ NOT PROVEN: no real eSIM installed on a handset, no usage row from a live SIM, no webhook received, role editors not reopened since the new toggles. First-line runbook = handoff §5.
- Drive-by finding (spawned as its own task): `delivery/audit.ts` passes `provider:"delivery"` which is not an IntegrationProvider enum member — its audit rows likely never write, swallowed by catch.
