# 2026-09-16 · TELNYX 10DLC REGISTRATION — BUILT + DEPLOYED, LIVE-PROVEN TO "READY TO FILE", NO REAL FILING YET

Full handoff: **`docs/ai-context/AGENT_HANDOFF_TELNYX_10DLC_2026-09-16.md`** (§000 = the build; §00/§0 = design revisions; §2 = Telnyx API facts)

- ✅ **Built end to end** (`85dee5bc` + wiring `34ff3aae`): admin page `/admin/texting-registration`
  (board + per-customer review/file/tracking), customer link `/texting-registration/<token>`
  (Loopcom logo, light/dark, customer EDITS ONLY legal name/type/EIN/IRS address/website + signs;
  carrier wording + account facts LOCKED), server-rendered `/texting-policy/<slug>` (the business's
  own privacy policy + SMS terms), invite email on the Loopcom shell with Izzy's copy
  (regulation → continue using SMS → fill out the 10DLC form → link), "File with Telnyx" that then
  runs brand → verify → campaign → carrier review → attach numbers → live unattended.
- ⛔ EIN = AES-GCM token bound to the registration, destroyed on verification / 14 days. Six keys,
  none default, page SUPER_ADMIN-forced. Separate tables from the wizard's TenantSmsRegistration.
- ⛔ **Charge = a SEPARATE uncharged $24 one-time invoice** (traced: never autopaid, never counts for
  the service cutoff, no email) — NOT a line on the next cycle invoice. Monthly fee/marketing shown nowhere.
- ✅ **Deployed** api `34ff3aae` (migration applied, `TEXTING_REGISTRATION_SWEEP_ARMED`) + portal
  (`9a04bb2c` — after two CSS-fix redeploys, verified in the shipped stylesheet). ✅ 41 tests incl. 300-customer stress (found+fixed a double-assign race).
  ✅ **Live in the container**: 25 concurrent submits → 1; EIN token has no digits; audited reveal;
  real invite email SENT; token flood 404 + per-IP 429; forged webhooks 401; TENANT_ADMIN 403.
- ⛔⛔ **The real browser found TWO bugs every test missed**: the public form couldn't scroll (portal locks html/body — `.tr-page` is its own scroller now) and ConnectSelect's inline 160px width pushed State into ZIP. Proven in Chrome: light/dark + logo, 10 errors, typed submit → thank-you, board, review page, audited EIN Show, Close.
- ⛔⛔ Worktree hazard: `2acf52b8` swept this module's uncommitted server.ts wiring (origin couldn't
  boot) → `5f279499` took it back → module committed on a private index, re-wired by pathspec,
  shared index re-synced (it showed the new files as deleted).
- ⏳ **NOT PROVEN: no real Telnyx filing** (needs a real EIN, $24) — brand/campaign/assign/charge/
  ready email only ran against the simulated registry; sole-prop PIN on a real phone; a human
  filling the form in a browser. Customer links are on **app.connectcomunications.com**
  (platform canonical origin) — Izzy may want loopcom.net.
- ⚠️ Platform issue seen during proof (NOT this feature, task chip spawned): after a portal deploy a browser on a long-lived HTTP/2 connection got 502s for minutes — old nginx workers "shutting down" for up to 7h still route to removed blue/green ports.
- ✅ **2026-09-16 late · NINE REAL CUSTOMER DRAFTS PREPARED for Izzy to send** (every tenant that sent SMS in the last 30 days): B Visible, Create A Box, Displaydex, Fixup Group, Gesheft, Hanna, Luxure Management, Relax Tires, Trust Bookkeepings — created through the live `POST /admin/texting-registration` (status `draft`, **0 links, 0 emails**; board counts `draft: 9`). Prefill = display name, business phone, email, numbers (+ owner name where the user row has one). Staff PATCH added websites that load (bvisible.us, displaydex.com, gesheftkosher.com — 403 to curl, bot wall) and fixed Gesheft's email (was TWO addresses comma-joined → `Contact@Gesheftkosher.com`). Legal name / type / EIN / IRS address left EMPTY — **no tenant has any of them stored anywhere** (TenantBillingSettings address columns and OnboardingSubmission are empty for all nine). ⛔ Gaps: 6 have no contact name (owner user rows carry none; staff route cannot set contact names), Fixup's reads "fix up"; trustbookkeepingny.com does not resolve (website left empty). ⛔⛔ **Every number is VoIP.ms** — the engine attaches only TELNYX `TenantSmsNumber` rows, so approval covers nothing until the numbers move to Telnyx. Filing = $24 each (Hanna is FREE — decide before filing). ⏳ No link sent yet — Izzy presses Create/Send per customer (a link can't be re-shown later: only its hash is stored).
- 2026-09-16 · **Izzy: "how do we move texting to Telnyx with no interruption?"** Answer given (advisory, nothing built): ORDER is the whole trick — file brand+campaign at Telnyx FIRST while the numbers keep texting on VoIP.ms (a campaign does not need its numbers to be approved), move a number ONLY after the campaign is live on every carrier, and attach it to the campaign the moment it lands. Inbound texts never break (ingest has no provider filter). The one unavoidable outbound gap = number lands at Telnyx → campaign assignment ASSIGNED (Telnyx: ~2h typical, up to 72h) — do it after hours. ⛔ Not wired today: nothing flips an existing customer's `TenantSmsNumber.provider` VOIPMS→TELNYX at landing (the carrier-migration watcher is source-guarded never to move texting), and this engine only re-checks moved numbers hourly — both need tracing before building. ⏳ UNVERIFIED: whether Telnyx lets a number be attached to a campaign before the port completes.
- ✅ **2026-09-16 night · THE SWITCHER IS BUILT + DEPLOYED (`56879d0f`, api `.build-commit` = 56879d0f, 0 restarts, `TEXTING_SWITCHER_ARMED` in the live log, first run 21:31Z flipped nothing — 18 VOIPMS / 1 TELNYX unchanged, 0 audit rows)** — Izzy: *"build the switcher … it will automatically detect when it's switched to Telnyx and kick in."* `apps/api/src/textingRegistration/switcher.ts` (wired in `wire.ts`, no server.ts change): every 5 min (boot kick 3 min, kill switch `TEXTING_SWITCHER_DISABLED=1`, boot line `TEXTING_SWITCHER_ARMED`) it reads the Telnyx account for every active VOIPMS `TenantSmsNumber` with a tenant; when the number is there AND `active` → texting profile set only if it has none ("Loopcom Sign-ups" resolver) → ONE conditional `updateMany where provider VOIPMS` flips it to TELNYX → `number_switched` event on the customer's 10DLC registration (History shows it, no carrier named) + `telnyx.texting_switched` audit → `advanceRegistration` kicked so an approved campaign attaches the number NOW (the engine's own attach code). ⛔ Blast radius TRACED: worker dispatch → TELNYX adapter; VoIP.ms inbox poll selects VOIPMS only (guarded); VoIP.ms DID sync upserts by phoneE164 and NEVER writes provider (guarded — if it ever did, every switched number would flip back); inbound ingest has no provider filter; telnyxPortWatchdog writes only TELNYX rows → no overlap. ✅ 13 tests (exactly-once test + write guard FAIL when the VOIPMS condition is removed); 10DLC suite 54/54; tsc clean in the three files. ✅ Dry run on prod before deploy: 18 VOIPMS rows, NONE active on Telnyx → deploy flips nothing today; Telnyx public key IS set (inbound works). ⛔ **845-723-1213 (Connect Communications) is `port-pending` on Telnyx → the switcher WILL flip it Friday 9/18 when it goes active** — Loopcom has no 10DLC campaign, so its outbound chat texts fail at Telnyx instead of VoIP.ms; platform pay-link/sign-in texting uses its own path (not traced here). ⛔ A tenant with no approved campaign is still flipped (VoIP.ms cannot send after a port anyway). ⏳ NOT PROVEN: a real number landing → flip → assigned → a real text sent and received.
- 2026-09-16 night · **GESHEFT GOES FIRST (Izzy).** Private 10DLC link created for Gesheft (registration `cmu4l96or0246ru13wg6jtq6r`, link `cmu4mqxuq066ipb12vypb6mqv`, expires 2026-10-16, NOT emailed — Izzy sends it himself); status now `awaiting_customer`. ⛔ Opening a link through `GET /texting-registration/:token` stamps `openedAt` — a staff check makes the board say "Opened"; my check's stamp was cleared by hand. Verify a link with the portal page's HTTP status, never the API read. Order from here: Gesheft fills the form → File with Telnyx → carriers approve → only THEN port 845-244-9666 (the switcher moves texting on landing). ⏳ The other eight drafts still have no link.
- 2026-09-16 night · **Relax Tires second.** Private 10DLC link created for Relax Tires (registration `cmu4l96zw024mru13ob191fd3`, expires 2026-10-16, NOT emailed — Izzy sends it); status `awaiting_customer`. Checked by portal page HTTP 200 only (no API read, so no false "Opened"). Number to port after approval: 845-776-1765.
- 2026-09-16 night · **Fixup Group third.** Private 10DLC link created (registration `cmu4l96lv0242ru13qrl2xa1x`, expires 2026-10-16, NOT emailed — Izzy sends it); status `awaiting_customer`; page HTTP 200. ⚠️ Its contact name shows "fix up" (account data). Number to port after approval: 845-806-7040.
- ✅ **2026-09-17 · TRUST BOOKKEEPINGS LINK CREATED (Izzy: "prepare me a 10DLC link for trust bookkeeping")** — through the deployed `POST /admin/texting-registration/cmu4l971x024qru13v8cv3rmw/link` (no email; Izzy sends it himself). Status `draft` → `awaiting_customer`; link expires **2026-10-17 15:29Z**; public page + its api both 200 from the outside — ⛔ and that outside GET of `/api/texting-registration/<token>` STAMPED the link `openedAt` 15:29:57Z, so the board shows Trust as "opened" before the customer has seen it (same for Gesheft + Relax last night; Fixup was not curled and reads unopened). Check a link through the portal PAGE only, never the api. ⛔ The raw URL is in the reply to Izzy ONLY — only its SHA-256 is stored, so it cannot be re-shown; pressing Create link again on the review page REVOKES this one. ⛔ Recipe for any staff call from the box: the container has NO `@prisma/client` at `/app` and no `jsonwebtoken` — mint HS256 from `$JWT_SECRET` (sub `cmm0414y7000tmq10nxtha90f`, role SUPER_ADMIN, tenantId null) and hit `127.0.0.1:3001` (pattern in loopcom `/tmp/preview.js`). Still empty for the customer to fill: legal name, entity type, EIN, IRS address, website (trustbookkeepingny.com does not resolve). ⛔ Its one number (+1 845-244-1708) is VoIP.ms — approval attaches nothing until it moves to Telnyx. Trust is the FOURTH link — Gesheft (21:45Z), Relax Tires (21:49Z) and Fixup (21:53Z) were created the night of 09-16 and handed to Izzy the same way (memory recorded it; this file had not). Still `draft` with no link: Displaydex, B Visible, Luxure, Hanna, Create A Box.
- ⛔⛔ **2026-09-18 · GESHEFT "DENIED" = NOT A CARRIER DENIAL — TELNYX REFUSED THE BRAND AT THE DOOR, nothing was created, nothing charged.** Pinches Meisels submitted the form 13:47Z (legal name "Gesheft " with a trailing space, "51 Forest Rd Suit 316-207, Monroe NY 10950", EIN entered) and picked **Business type = "Publicly traded company" (PUBLIC_PROFIT)**. Izzy pressed File with Telnyx twice (13:55:30Z, 13:55:54Z); both times `POST /10dlc/brand` came back `400 10015: stockExchange, stockSymbol, and businessContactEmail are required for public profit entity.` The engine put it back to `submitted` ("Ready to file") with `lastError`; `telnyxBrandId`/`chargeAddedAt`/`brandCreateStartedAt` all EMPTY — no $24, no brand, no campaign. **Product hole:** the public form OFFERS "Publicly traded company" but never collects the three fields Telnyx demands for it, and `runFilingChecks` does not fail on it (handoff §2 recorded the requirement; it never reached the form/checks). Fix for Gesheft = review page → "Fill it in myself" → Business type "LLC or corporation" (staff edit is allowed in `submitted`) → File again; also confirm the EXACT IRS (CP-575) legal name — "Gesheft" alone will likely come back UNVERIFIED. ⏳ Not refiled, not changed (customer's own attestation — Izzy decides); form hole not closed yet.
