# 2026-09-14 · Solidify Concrete ext 101 (Shlomo) added to TestFlight

Full handoff: `docs/ai-context/AGENT_HANDOFF_SOLIDIFY_CONCRETE_TESTFLIGHT_2026-09-14.md`

- Izzy asked for "solidified concrete 101". The Connect tenant is named **Solidify Concrete** (`cmnlgryjz0006p9pa60892fi9`). Ext 101 is "Shlomo", user **sstern@solidifyconcrete.com** (USER, INVITED, never logged in).
- ✅ Added to TestFlight group "Loopcom Testers" (`fe508ee6…`): POST `/v1/betaTesters` → 201, and Apple shows the state as **INVITED** right away, so Apple sent the TestFlight email. Builds 59/58/56 are VALID and attached.
- The portal `USER_INVITE` (the create-password email) had already been SENT to him earlier the same day (19:16 UTC), so it was NOT sent again.
- firstName "Shlomo" came from the extension. **lastName was left blank on purpose.** "Stern" is only a guess from the address, and a betaTester's name can't be changed without deleting and re-adding, which fires a second invite.
- ✅ **Ext 102 "Office"** too (same day): **office@solidifyconcrete.com** added → 201, **INVITED**. No name set, because "Office" isn't a person. This address is also the billing email and owns ext 103 (Toby Horowitz). Its portal USER_INVITE was already SENT at 19:17 UTC, so it wasn't re-sent. Re-send script: `asc-invite-scoffice.mjs`.
- ✅ **TEXTING ON, SHARED INBOX (same day, Izzy: "turn on text message… shared mailbox in both extensions")**: `TenantSmsNumber cmogdrtil007ypk5ey83h47e5` **+1 845-557-7879** (their only DID, PBX inbound 357) → tenant, both assigned fields **null** = shared, tenant default, active, via the real `PATCH /admin/apps/voip-ms/numbers/:id` → 200. Carrier already read `sms_enabled "1"` (routing `344022_solidefyc`), so there was no carrier write. The poller picked it up next cycle (numbers 16→17). ⛔ **`sms_email` forwards every inbound text to izzwgg@gmail.com** (enabled), left untouched as Izzy's call. ⛔ **Billing NOT enabled**: `smsBillingEnabled` false, not flat-rate, so turning it on adds $10/mo. Needs Izzy's word.
- ✅ **2-month backfill**: 14 inbound texts (2026-07-30 → 09-14) into **12 threads**, all `smsInboxOwnerUserId` empty. **Both** office@ and sstern@ are participants on all 12. 0 duplicate provider ids, no pushes sent. VoIP.ms had no outbound texts to backfill. ⛔ The first two runs returned 522 and then 1 row: per-DID getSMS needs `from`, not `date_from` ([[voipms-getsms-wants-from-not-date-from]]).
- ✅ **443**: `webrtcRouteViaSbc` false→**true**, `sipWsUrl` `wss://m.connectcomunications.com:8089/ws` → **`wss://sip.connectcomunications.com/sip`** (the pinned-existing-customer shape, same as B Visible/Gesheft/etc.; the hostname answered 101 from loopcom first). Backup: `/root/solidify-sip-backup-20260914.json`. ⏳ **Nobody has moved yet**: the only live registration, `T14_102_1` (wss, from 38.105.205.181), keeps its cached 8089 URL until the user signs out and back in. No MobileDevice rows exist, and no desk phone is registered (desk phones register on 5060 regardless of this flag).
- ⏳ NOT PROVEN: he hasn't accepted or installed yet. If he says no email arrived: `node /root/.appstoreconnect/asc-invite-sstern.mjs` on loopcom. If his Apple Account uses a different email, add that address as its own tester.
