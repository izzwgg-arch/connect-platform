<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.BLUETOOTH"/>
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
  <uses-permission android:name="android.permission.CAMERA"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
  <!--
    Required on Android 14+ so SipKeepAliveService can run as a microphone-typed
    foreground service while a call is active. Without this, Android silently
    suppresses microphone capture the moment the app loses foreground (the
    remote party hears silence). The permission is benign at install time —
    it gates a foreground-service type, not the mic itself (RECORD_AUDIO
    still gates actual capture).
  -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.MANAGE_OWN_CALLS"/>
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
  <uses-permission android:name="android.permission.VIBRATE"/>
  <uses-permission android:name="android.permission.WAKE_LOCK"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW"/>
      <category android:name="android.intent.category.BROWSABLE"/>
      <data android:scheme="https"/>
    </intent>
  </queries>
  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="true" android:theme="@style/AppTheme">
    <meta-data android:name="com.google.firebase.messaging.default_notification_color" android:resource="@color/notification_icon_color"/>
    <meta-data android:name="com.google.firebase.messaging.default_notification_icon" android:resource="@drawable/notification_icon"/>
    <meta-data android:name="expo.modules.notifications.default_notification_color" android:resource="@color/notification_icon_color"/>
    <meta-data android:name="expo.modules.notifications.default_notification_icon" android:resource="@drawable/notification_icon"/>
    <meta-data android:name="expo.modules.updates.ENABLED" android:value="true"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH" android:value="NEVER"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS" android:value="0"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATE_URL" android:value="https://u.expo.dev/53c72ced-180c-4885-a3ff-7d5da5717ead"/>
    <meta-data android:name="expo.modules.updates.EXPO_RUNTIME_VERSION" android:value="@string/expo_runtime_version"/>
    <!--
      IncomingCallFirebaseService MUST run in the default (main) process — the
      same process as FirebaseInitProvider — otherwise FCM silently drops
      every push. Firebase only routes MESSAGING_EVENT intents to a service
      whose process has been initialized via FirebaseApp.initializeApp(),
      which only happens automatically in the process that hosts
      FirebaseInitProvider (the default process).

      We previously declared this service in `:keepalive` to survive recents
      swipe, but the side effect was zero FCM deliveries — every wake push
      reached Expo/FCM successfully but never invoked onMessageReceived.
      The service still wakes the main process via FCM (Android starts the
      process automatically on incoming push), and from there it kicks
      SipKeepAliveService (which DOES live in `:keepalive` for FGS
      isolation) so the SIP REGISTER stays alive after the wake completes.
    -->
    <service android:name="com.connectcommunications.mobile.IncomingCallFirebaseService" android:exported="false">
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT"/>
      </intent-filter>
    </service>
    <!--
      foregroundServiceType bitmask: declared types this service is ALLOWED to
      foreground itself with at runtime. We add `microphone` so that during an
      active call the service can re-foreground itself with the microphone
      type and Android 14+ will keep the WebRTC mic capture alive even when
      the app is backgrounded. `phoneCall` keeps CallKeep / Telecom integration
      happy; `dataSync` is the always-safe fallback when PHONE_CALL is rejected.
    -->
    <service android:name="com.connectcommunications.mobile.SipKeepAliveService" android:exported="false" android:foregroundServiceType="phoneCall|dataSync|microphone" android:process=":keepalive"/>
    <receiver android:name="com.connectcommunications.mobile.KeepAliveRestartReceiver" android:enabled="true" android:exported="false" android:process=":keepalive"/>
    <receiver android:name="com.connectcommunications.mobile.BootReceiver" android:enabled="true" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED"/>
        <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
        <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
        <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
      </intent-filter>
    </receiver>
    <service android:name="expo.modules.notifications.service.ExpoFirebaseMessagingService" tools:node="remove"/>
    <service android:name="io.wazo.callkeep.VoiceConnectionService" android:label="Calls" android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE" android:exported="true" android:foregroundServiceType="phoneCall">
      <intent-filter>
        <action android:name="android.telecom.ConnectionService"/>
      </intent-filter>
    </service>
    <!--
      Connect's own SELF_MANAGED Telecom ConnectionService. Registered so the
      OS can spawn ConnectConnectionService → ConnectIncomingConnection when
      TelecomManager.addNewIncomingCall() is called from
      IncomingCallFirebaseService on FCM data pushes — the only path that
      reliably wakes a fully killed app on Android 14/15+ across Samsung One
      UI, Pixel, OnePlus, Xiaomi MIUI etc. (Background Activity Launch
      restrictions silently drop direct startActivity() calls; full-screen
      intents work but require the user to leave POST_NOTIFICATIONS +
      USE_FULL_SCREEN_INTENT enabled — Telecom is the OEM-canonical wake
      path and bypasses both restrictions.)
    -->
    <service android:name="com.connectcommunications.mobile.ConnectConnectionService" android:label="Connect Voice" android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE" android:exported="true" android:foregroundServiceType="phoneCall|microphone">
      <intent-filter>
        <action android:name="android.telecom.ConnectionService"/>
      </intent-filter>
    </service>
    <activity android:name=".MainActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode" android:launchMode="singleTask" android:windowSoftInputMode="adjustResize" android:theme="@style/Theme.App.SplashScreen" android:exported="true" android:screenOrientation="portrait" android:showWhenLocked="true" android:turnScreenOn="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="com.connectcommunications.mobile"/>
        <data android:scheme="exp+connect-mobile"/>
      </intent-filter>
    </activity>
    <activity android:name="com.facebook.react.devsupport.DevSettingsActivity" android:exported="false"/>
  </application>
</manifest>