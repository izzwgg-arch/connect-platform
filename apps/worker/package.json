{
  "name": "@connect/worker",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo lint-worker",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "vm-backfill": "tsx src/scripts/voicemail-spool-backfill.ts",
    "vm-reconcile-last": "tsx src/scripts/voicemail-reconcile-last.ts"
  },
  "dependencies": {
    "@connect/db": "workspace:*",
    "@connect/shared": "workspace:*",
    "@connect/integrations": "workspace:*",
    "bullmq": "^5.58.0",
    "ioredis": "^5.8.0",
    "@connect/security": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
