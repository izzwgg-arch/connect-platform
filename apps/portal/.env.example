# Optional in dev: browser calls the API directly (must be reachable; CORS is not enabled on the API).
# If unset, `next dev` proxies same-origin `/api/*` → http://127.0.0.1:3001 (see next.config.js rewrites).
NEXT_PUBLIC_API_URL=

# When port 3001 is used by another app, point the dev proxy at Connect API on another port, e.g.:
# PORTAL_API_DEV_PROXY=http://127.0.0.1:3011
# Then start the API with PORT=3011 (PowerShell: $env:PORT='3011'; pnpm --filter @connect/api dev)
PORTAL_API_DEV_PROXY=

# Optional: WebSocket URL for the telephony real-time service (AMI event stream).
# When set, the portal connects for live call/extension/queue state updates.
# Must use wss:// in production (nginx proxies external wss: → internal ws:).
# Example (production):  wss://app.connectcomunications.com/ws/telephony
# Example (dev):         ws://localhost:3003/ws/telephony
# Leave blank to fall back to existing API polling mode.
NEXT_PUBLIC_TELEPHONY_WS_URL=
