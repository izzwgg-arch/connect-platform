"use client";

/** Replaced in full by the feed domain build (see docs/community/CONVENTIONS.md). */
export function FeedHome() {
  return (
    <div className="col">
      <div className="card">
        <div className="ct">Home</div>
        <p className="dim">Your feed is being built.</p>
      </div>
    </div>
  );
}
