# Copy to .env and fill in the real database URL (through the SSH tunnel).
# The tunnel (tunnel.cmd) forwards the server's Postgres to 127.0.0.1:15432.
# NEVER commit the real .env — it holds the database password.
DATABASE_URL=postgresql://USER:PASSWORD@127.0.0.1:15432/connectcomms
